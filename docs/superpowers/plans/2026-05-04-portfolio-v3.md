# 정인수 포트폴리오 v3 — 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 정인수님의 포트폴리오 사이트 v3를 신규 빌드한다 — 회장이 5분 면접 시뮬레이션을 통과시킬 수 있는 페이퍼 갤러리(라이트) + 키네틱 다크 듀얼 디자인의 트렌디 멀티페이지 Next.js 앱.

**Architecture:** Next.js 16 App Router + Tailwind v4 + GSAP ScrollTrigger + Lenis + View Transitions API. 6 임팩트 스트림 중심 `/career`, 3 Tier 강의 `/marketing/teaching`, 트렌디·풍부 6 SaaS 디테일 페이지, make.com 4 핵심 + 81 보조 분리. Playwright Node 스크립트로 50~70 라이브 URL 일괄 캡처.

**Tech Stack:** Next.js 16.x · React 19 · Tailwind v4 · TypeScript 5.9 · Pretendard Variable · Fraunces Variable (7 axes) · JetBrains Mono · GSAP 3.12 · Lenis 1.x · Framer Motion 12 · Recharts 2.x · Visx 3.x · Lucide React · shadcn/ui (Radix) · sharp · Playwright · Vitest

**Spec:** `docs/superpowers/specs/2026-05-04-portfolio-v3-design.md`

---

## File Structure (Decomposition)

```
junginsu-portfolio-v3/
├── app/
│   ├── layout.tsx                       # Theme + Lenis + Nav + Footer + View Transitions
│   ├── globals.css                      # Tailwind v4 @theme + 폰트 + 유틸
│   ├── page.tsx                         # 홈 600vh
│   ├── about/page.tsx
│   ├── career/page.tsx
│   ├── marketing/
│   │   ├── page.tsx
│   │   ├── teaching/page.tsx
│   │   └── content/page.tsx
│   ├── builder/
│   │   ├── page.tsx                     # 인덱스
│   │   ├── [slug]/page.tsx              # 6 SaaS 디테일 (트렌디 풍부)
│   │   ├── scenarios/page.tsx
│   │   └── github/page.tsx
│   └── contact/page.tsx
├── components/
│   ├── ui/                              # shadcn primitives (button/card/dialog/sheet/tabs/tooltip/switch)
│   ├── layout/                          # nav, footer, theme-toggle
│   ├── motion/                          # lenis-provider, magnetic, kinetic-text, counter, view-transition, color-sweep, pin-section
│   ├── home/                            # 6 핀 섹션
│   ├── about/                           # chapters, duality-venn
│   ├── career/                          # impact-streams, timeline-toggle, certifications
│   ├── marketing/                       # kpi-charts, ai-clients, teaching-carousel, teaching-tiers, content-grid
│   ├── builder/                         # saas-index, saas-card-5bullet, saas-detail/*, scenarios, github-categories
│   ├── viz/                             # korea-map, flow-diagram, treemap-81, venn-diagram
│   └── shared/                          # divider, lightbox, magnetic-button
├── lib/
│   ├── data/                            # career, certifications, saas, scenarios, teaching, content-refs, github
│   ├── theme.ts                         # use-theme hook + provider
│   ├── motion.ts                        # GSAP setup
│   ├── lenis.ts                         # Lenis singleton hook
│   ├── utils.ts                         # cn helper
│   └── captured-meta.json               # generated by capture-urls
├── scripts/
│   ├── url-list.json                    # 캡처 대상
│   ├── capture-urls.ts                  # Playwright 일괄
│   └── optimize-images.ts               # sharp webp/avif
├── public/
│   ├── photos/profile-hero.jpg
│   ├── photos/teaching/*                # 56장
│   ├── certifications/*                 # 9 + PDF
│   ├── automation/*                     # 11 + PDF 2
│   ├── saas-folders/{6 slug}/*          # 57
│   ├── captured/{slug}/{desktop,mobile}.jpg  # generated
│   ├── content-refs/*
│   └── resume.pdf
├── tests/data/                          # vitest data integrity
└── 설정 (package.json · tsconfig.json · next.config.ts · postcss.config.mjs)
```

**파일 책임 원칙**: 각 파일 1 책임. 모션 primitives는 `components/motion/`에 격리, 데이터는 `lib/data/`에 순수 객체로 격리 (test 가능). 페이지는 컴포넌트 조합만.

---

## Phase 0 — Foundation (프로젝트 초기화)

### Task 0.1: Next.js 16 + Tailwind v4 + TypeScript 프로젝트 init

**Files:**
- Create: `package.json`, `tsconfig.json`, `next.config.ts`, `postcss.config.mjs`, `app/layout.tsx`, `app/page.tsx`, `app/globals.css`, `.gitignore`

- [ ] **Step 1: 디렉토리에서 init 명령 실행**

```bash
cd /home/a20616050/projects/A/junginsu-portfolio-v3
pnpm create next-app@latest . --ts --tailwind --app --turbopack --no-eslint --no-src-dir --import-alias "@/*" --use-pnpm
```

질문에 yes/no 응답 → 마지막에 빈 페이지 + Tailwind v4 셋업 완료.

- [ ] **Step 2: 결과 확인**

Run: `ls -la app/ && cat package.json | head -30`
Expected: `app/layout.tsx`, `app/page.tsx`, `app/globals.css`, `next.config.ts` 생성

- [ ] **Step 3: 첫 commit (foundation 시작점)**

```bash
git init -b main
git add -A
git commit -m "chore: init next.js 16 + tailwind v4 + typescript"
```

### Task 0.2: 모션·차트·아이콘 의존성 설치

**Files:** Modify `package.json`

- [ ] **Step 1: 핵심 의존성 설치**

```bash
pnpm add gsap lenis framer-motion lucide-react recharts @visx/treemap @visx/hierarchy @visx/scale @visx/responsive zod clsx tailwind-merge
```

- [ ] **Step 2: shadcn/ui 의존성 설치 (Radix + 유틸)**

```bash
pnpm add @radix-ui/react-dialog @radix-ui/react-tabs @radix-ui/react-tooltip @radix-ui/react-switch @radix-ui/react-slot class-variance-authority
```

- [ ] **Step 3: 빌드·테스트 도구**

```bash
pnpm add -D playwright sharp vitest @vitest/ui jsdom @types/node
pnpm exec playwright install chromium
```

- [ ] **Step 4: 결과 검증**

Run: `cat package.json | grep -A 30 dependencies`
Expected: gsap, lenis, framer-motion, lucide-react, recharts, @visx, sharp, playwright, vitest 모두 dependencies 또는 devDependencies에 등장.

- [ ] **Step 5: Commit**

```bash
git add package.json pnpm-lock.yaml
git commit -m "chore: add motion + viz + shadcn + playwright deps"
```

### Task 0.3: 폴더 구조 생성

**Files:**
- Create: `components/{ui,layout,motion,home,about,career,marketing,builder,viz,shared}/.gitkeep`
- Create: `lib/{data}/.gitkeep`
- Create: `scripts/.gitkeep`
- Create: `public/{photos,certifications,automation,saas-folders,captured,content-refs}/.gitkeep`
- Create: `tests/data/.gitkeep`

- [ ] **Step 1: 디렉토리 일괄 생성**

```bash
mkdir -p components/{ui,layout,motion,home,about,career,marketing,builder/saas-detail,viz,shared}
mkdir -p lib/data
mkdir -p scripts
mkdir -p public/{photos/teaching,certifications,automation,saas-folders/{tickpoint,lumio,os-agent,mkt-automation,propintel,architect},captured,cropped,content-refs}
mkdir -p tests/data
for d in components/* lib/data scripts public/photos public/photos/teaching public/certifications public/automation public/saas-folders/{tickpoint,lumio,os-agent,mkt-automation,propintel,architect} public/captured public/cropped public/content-refs tests/data; do
  touch "$d/.gitkeep"
done
```

- [ ] **Step 2: 결과 확인**

Run: `find components lib scripts public tests -type d | sort`
Expected: 모든 디렉토리 존재

- [ ] **Step 3: Commit**

```bash
git add components lib scripts public tests
git commit -m "chore: scaffold project folder structure"
```

### Task 0.4: Tailwind v4 디자인 토큰 (라이트 + 다크 듀얼)

**Files:** Modify `app/globals.css`

- [ ] **Step 1: globals.css 작성**

```css
@import "tailwindcss";

@theme {
  /* === LIGHT (페이퍼 갤러리) === */
  --color-paper: #F5F1E8;
  --color-ink: #1B1B1B;
  --color-orange: #FF6B02;
  --color-stone: #5C544A;
  --color-line: rgb(27 27 27 / 0.18);

  /* === DARK (잉크 키네틱) === */
  --color-paper-dark: #0A0A0A;
  --color-ink-dark: #F4F0E6;
  --color-orange-dark: #FF7A1F;
  --color-stone-dark: #A89F8E;
  --color-line-dark: rgb(244 240 230 / 0.20);

  /* === 폰트 스택 === */
  --font-display: "Fraunces", "Pretendard Variable", system-ui, serif;
  --font-body: "Pretendard Variable", -apple-system, "Apple SD Gothic Neo", "Noto Sans KR", system-ui, sans-serif;
  --font-mono: "JetBrains Mono", ui-monospace, monospace;

  /* === 이징 === */
  --ease-default: cubic-bezier(0.6, 0.05, 0.3, 0.95);
  --ease-page: cubic-bezier(0.85, 0, 0.15, 1);
  --ease-kinetic: cubic-bezier(0.34, 1.56, 0.64, 1);
}

/* 폰트 — Pretendard CDN (Variable, dynamic-subset) */
@import url("https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable-dynamic-subset.min.css");

@layer base {
  :root {
    --bg: var(--color-paper);
    --fg: var(--color-ink);
    --accent: var(--color-orange);
    --mute: var(--color-stone);
    --line: var(--color-line);
  }

  :root.dark, [data-theme="dark"] {
    --bg: var(--color-paper-dark);
    --fg: var(--color-ink-dark);
    --accent: var(--color-orange-dark);
    --mute: var(--color-stone-dark);
    --line: var(--color-line-dark);
  }

  html {
    background: var(--bg);
    color: var(--fg);
    font-family: var(--font-body);
    font-feature-settings: "ss03", "cv11";
    font-optical-sizing: auto;
    letter-spacing: -0.01em;
    transition: background 200ms var(--ease-default), color 200ms var(--ease-default);
  }

  body {
    line-height: 1.7;
  }

  ::selection {
    background: var(--accent);
    color: var(--bg);
  }
}

/* 타이포 위계 8단계 */
@layer utilities {
  .text-display-mega { font-family: var(--font-display); font-size: clamp(96px, 18vw, 240px); line-height: 0.92; letter-spacing: -0.04em; font-weight: 500; }
  .text-display-xl   { font-family: var(--font-display); font-size: clamp(64px, 10vw, 160px); line-height: 0.94; letter-spacing: -0.03em; font-weight: 500; }
  .text-display-lg   { font-family: var(--font-display); font-size: clamp(48px, 6vw, 96px); line-height: 1.0; letter-spacing: -0.025em; font-weight: 500; }
  .text-display-md   { font-family: var(--font-display); font-size: clamp(32px, 4vw, 56px); line-height: 1.1; letter-spacing: -0.02em; font-weight: 500; }
  .text-body-xl      { font-size: clamp(22px, 2.4vw, 28px); line-height: 1.5; }
  .text-body-lg      { font-size: 20px; line-height: 1.6; }
  .text-body         { font-size: 17px; line-height: 1.7; letter-spacing: -0.01em; }
  .text-meta         { font-size: 13px; line-height: 1.4; letter-spacing: 0.08em; text-transform: uppercase; font-family: var(--font-mono); }
}

/* 마우스 커서 (마그네틱 wrapper에서 활성화) */
.cursor-magnetic { cursor: pointer; transition: transform 0.4s var(--ease-default); }
```

- [ ] **Step 2: dev 서버 띄우고 결과 확인**

```bash
pnpm dev
```

브라우저에서 http://localhost:3000 → 기본 Next.js 페이지가 페이퍼(#F5F1E8) 베이스에 차콜 텍스트로 표시되는지 확인.

- [ ] **Step 3: Commit**

```bash
git add app/globals.css
git commit -m "feat(theme): tailwind v4 tokens + 라이트/다크 듀얼 + 8단계 타이포"
```

### Task 0.5: 폰트 (Fraunces + JetBrains Mono via next/font)

**Files:** Modify `app/layout.tsx`

- [ ] **Step 1: app/layout.tsx 폰트 setup**

```tsx
import type { Metadata } from "next";
import { Fraunces, JetBrains_Mono } from "next/font/google";
import "./globals.css";

const fraunces = Fraunces({
  subsets: ["latin"],
  variable: "--font-fraunces",
  axes: ["SOFT", "WONK", "opsz"],
  display: "swap",
});

const jetbrains = JetBrains_Mono({
  subsets: ["latin"],
  variable: "--font-jetbrains",
  display: "swap",
});

export const metadata: Metadata = {
  title: "정인수 — AI 시대를 만난 17년 마케터, 풀사이클 빌더",
  description: "AI Builder · 마케팅 17년 · GitHub 84 저장소 · make.com 4 핵심 시나리오 · 6 라이브 SaaS · KOICA·정부 부처 AI 강의",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="ko" className={`${fraunces.variable} ${jetbrains.variable}`} suppressHydrationWarning>
      <body>{children}</body>
    </html>
  );
}
```

- [ ] **Step 2: globals.css 폰트 변수 매핑**

`app/globals.css`의 `@theme` 안 폰트 정의를 다음으로 교체:

```css
--font-display: var(--font-fraunces), "Pretendard Variable", system-ui, serif;
--font-body: "Pretendard Variable", -apple-system, "Apple SD Gothic Neo", "Noto Sans KR", system-ui, sans-serif;
--font-mono: var(--font-jetbrains), ui-monospace, monospace;
```

- [ ] **Step 3: dev 서버 새로고침 + 폰트 확인**

브라우저 dev tools → Network → Fonts 탭 → Fraunces · JetBrains Mono · pretendard 모두 200 OK.

- [ ] **Step 4: Commit**

```bash
git add app/layout.tsx app/globals.css
git commit -m "feat(theme): Fraunces 7-axis + JetBrains Mono via next/font + Pretendard CDN"
```

---

## Phase 1 — 데이터 레이어 (TDD-friendly, vitest)

> **원칙**: 데이터는 순수 TypeScript 객체로 분리 → vitest로 무결성 검증. 페이지는 데이터를 import만.

### Task 1.1: vitest 설정

**Files:** Create `vitest.config.ts`, modify `package.json`

- [ ] **Step 1: vitest.config.ts 작성**

```ts
import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    environment: "node",
    include: ["tests/**/*.test.ts"],
    globals: true,
  },
});
```

- [ ] **Step 2: package.json scripts 추가**

`package.json`의 `"scripts"`에 추가:

```json
"test": "vitest run",
"test:watch": "vitest",
"capture": "tsx scripts/capture-urls.ts",
"optimize": "tsx scripts/optimize-images.ts"
```

`pnpm add -D tsx` 추가 설치.

- [ ] **Step 3: 결과 확인**

```bash
pnpm test --run
```
Expected: "No test files found" (정상 — 아직 테스트 없음)

- [ ] **Step 4: Commit**

```bash
git add vitest.config.ts package.json pnpm-lock.yaml
git commit -m "chore: add vitest + tsx for data tests"
```

### Task 1.2: Career 데이터 (6 임팩트 스트림 + 17 회사 풀)

**Files:**
- Create: `lib/data/career.ts`
- Test: `tests/data/career.test.ts`

- [ ] **Step 1: 실패 테스트 작성**

`tests/data/career.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import { IMPACT_STREAMS, CAREER_FULL } from "@/lib/data/career";

describe("Career Data", () => {
  it("6 임팩트 스트림 정확히 정의", () => {
    expect(IMPACT_STREAMS).toHaveLength(6);
    expect(IMPACT_STREAMS[0].title).toContain("AI Builder");
    expect(IMPACT_STREAMS[3].title).toContain("광고 운영");
  });

  it("17 회사 풀 타임라인 정확히 정의 (이력서 기준)", () => {
    expect(CAREER_FULL).toHaveLength(17);
    expect(CAREER_FULL[0].company).toBe("팔레트 ㈜");
    expect(CAREER_FULL[0].current).toBe(true);
  });

  it("글로리아교육재단 임팩트 표현 — 자동화 X, 정확한 마케팅 표현", () => {
    const gloria = CAREER_FULL.find(c => c.company.includes("글로리아"));
    expect(gloria).toBeDefined();
    expect(gloria!.impact).not.toContain("자동화");
    expect(gloria!.impact).toContain("블로그");
    expect(gloria!.impact).toContain("입학");
  });

  it("출처 미검증 데이터 사용 X (TMON ROAS / NGO 마음하나)", () => {
    const allText = JSON.stringify({ IMPACT_STREAMS, CAREER_FULL });
    expect(allText).not.toContain("7,404");
    expect(allText).not.toContain("4,378");
    expect(allText).not.toContain("마음하나");
  });
});
```

- [ ] **Step 2: 실패 확인**

```bash
pnpm test --run tests/data/career.test.ts
```
Expected: FAIL "Cannot find module '@/lib/data/career'"

- [ ] **Step 3: 데이터 구현**

`lib/data/career.ts`:

```ts
export type ImpactStream = {
  id: string;
  number: string;
  title: string;
  bullets: string[];
  companies: string[];
};

export type Company = {
  period: string;
  company: string;
  role: string;
  salary?: string;
  impact: string;
  current?: boolean;
};

export const IMPACT_STREAMS: ImpactStream[] = [
  {
    id: "ai-builder",
    number: "01",
    title: "AI Builder · 풀사이클 자동화",
    bullets: [
      "100+ Vibe Coding 자체 시스템 1인 개발 + GitHub 84 저장소",
      "make.com 4 핵심 시나리오 (정부지원사업·10X 콘텐츠·계약서+GA4·뉴스레터/웹 크롤링) + n8n",
      "데이터 크롤링·OCR·웹스크랩·자동 발행 + AI Prompt SEO",
      "멀티 LLM 통합 (Claude · GPT · Gemini · Perplexity)",
    ],
    companies: ["팔레트 ㈜ Agent 본부 팀장", "솔찍한인쌤 (병행)"],
  },
  {
    id: "ai-saas-pl",
    number: "02",
    title: "AI SaaS PL · 클라이언트 4건 진행 중",
    bullets: [
      "호반그룹 · 서울법무법인 · 아주그룹 · Palette OS Agent",
      "클라이언트 시스템 기획 + 개발 리딩",
    ],
    companies: ["팔레트 ㈜"],
  },
  {
    id: "ai-content-op",
    number: "03",
    title: "AI Content Operation · 7+ 클라이언트",
    bullets: [
      "성동청년이룸창업지원센터 · 고려대기술지주 · 제2서울핀테크랩 · 서울도시철도엔지니어링 · 시스트란 · 모두솔루션 · 리부트라이프",
      "콘텐츠 자동화 + 데이터 기반 마케팅 전략 운영",
    ],
    companies: ["팔레트 ㈜"],
  },
  {
    id: "ad-ops",
    number: "04",
    title: "광고 운영·총괄 (10년+ 누적)",
    bullets: [
      "티몬 공식 광고대행, 연 40~60억 광고비 총괄 (9개월)",
      "전국 30지점·카페 4지점, 연 20억+ 광고비 총괄 (1년 3개월)",
      "월 3,000만원 광고비 절감 + 네이버 검색광고·SNS 각 월 1억+",
      "Naver · Daum · Kakao · Google · SNS · GDN 통합 운영",
    ],
    companies: ["퍼포먼스디자인", "퍼스트 아카데미", "프랜차이즈산업연구원"],
  },
  {
    id: "pm",
    number: "05",
    title: "종합홍보 PM · 20+ 프로젝트",
    bullets: [
      "농림축산식품부 · 조달청 · 한국벤처투자 · 창업진흥원 (2023 과업 총괄 PM) + 14건+",
      "마케팅·홍보 PM + 마케팅실 직원·AI Agent 직원 관리",
      "Owned Media (Blog·SNS·YouTube) + Paid Media (Naver·Kakao·Google·Instagram·Facebook·GDN)",
    ],
    companies: ["Capsule Media (팔레트 그룹)"],
  },
  {
    id: "teaching",
    number: "06",
    title: "마케팅 강의 · 7년 6개월 (병행)",
    bullets: [
      "AI 강의 — 정부 부처 · 창업지원센터 (성동청년이룸·한국산림복지진흥원·제2서울핀테크랩·고려대기술지주)",
      "마케팅 강의 — KOICA·대학·소상공인·기업·자영업자",
      "블로그 기초 → 마케팅 활용 / 지식인 / 검색광고 / 플레이스 / 온라인 마케팅 기획·운영",
    ],
    companies: ["솔찍한인쌤"],
  },
];

export const CAREER_FULL: Company[] = [
  { period: "2023.04~ 재직중", company: "팔레트 ㈜", role: "Agent 본부 팀장", salary: "6,500만원", impact: "AI Builder 100+ 자체 시스템 / AI SaaS PL 4건 / AI Content Operation 7+ 클라이언트", current: true },
  { period: "2018.09~2026.02 (7년 6개월)", company: "솔찍한인쌤", role: "프리랜서 (병행)", salary: "2,000만원", impact: "AI 강의 (정부 부처·창업지원센터) + 마케팅 강사 (KOICA·대학·소상공인·기업·자영업자)" },
  { period: "2022.08~2023.04 (9개월)", company: "퍼포먼스디자인", role: "운영총괄실장", salary: "6,000만원", impact: "티몬 공식 광고대행 연 40~60억 광고비 총괄 + 화장품·가전·성형외과 마케팅 + 나라장터 제안" },
  { period: "2022.05~2022.08 (4개월)", company: "프랜차이즈산업연구원", role: "마케팅 책임연구원/실장", salary: "5,500만원", impact: "광고대행사 마케팅 전략실" },
  { period: "2022.06~2022.08 (3개월)", company: "㈜브랜딩파트너스", role: "단기", impact: "—" },
  { period: "2021.09~2022.06 (10개월)", company: "㈜한국바리스타자격검정협회", role: "단기", impact: "—" },
  { period: "2021.03~2022.05 (1년 3개월)", company: "퍼스트 아카데미 본사", role: "마케팅 총괄팀장", salary: "4,600만원", impact: "전국 30지점·카페 4지점 연 20억+ 광고비 / 월 3,000만원 절감 / 네이버 검색광고·SNS 각 월 1억+" },
  { period: "2020.06~2021.03 (10개월)", company: "중앙대학교 사회교육처", role: "실장", salary: "4,200만원", impact: "평생교육원 입학·홍보 모집 총괄 + 광고대행사 관리 + DA·네이버 애널리틱스" },
  { period: "2017.06~2020.05 (3년)", company: "글로리아교육재단 ㈜", role: "입학관리처 팀장 3년차", salary: "4,200만원", impact: "한국항공전문/국제호텔전문 — 블로그·지식인·파워링크·파워컨텐츠 + 전국 고교·학원 방문 홍보 + B2C 입학상담·유치" },
  { period: "2017.01~2017.05 (5개월)", company: "(강남에듀)강남직업전문학교", role: "입학관리처 팀장 1년차", salary: "3,700만원", impact: "검색광고·홈페이지 리뉴얼·과정별 안내문·MOU" },
  { period: "2015.07~2016.09 (1년 3개월)", company: "설봉학원 SB사이버평생교육원", role: "교학지원팀 과장", salary: "3,200만원", impact: "학점은행제 + B2B 기업협약" },
  { period: "2013.11~2015.07 (1년 9개월)", company: "KG 패스원 사회교육원", role: "평생교육사업부 대리", salary: "3,600만원", impact: "매출 1~2위 유지 + 최대 1,500명 회원관리" },
  { period: "2012.11~2013.10 (1년)", company: "이페이저 팬에듀케이션", role: "교육마케팅 사원", salary: "2,500만원", impact: "입사 4개월차 신규매출 1위" },
  { period: "2012.04~2012.09 (6개월)", company: "㈜재능교육", role: "학습지선생님", salary: "2,800만원", impact: "유아~중등 교육·학부모 상담" },
  { period: "2010.03~2012.02 (2년)", company: "영동대학교 학과조교", role: "사원 2년차", salary: "1,800만원", impact: "우수교직원 수상 + 학교·학과 행정 + 재학생 관리" },
  { period: "2006.07~2006.08 (2개월)", company: "㈜제원인터내쇼날", role: "단기", impact: "—" },
  { period: "2001.10~2002.11 (1년 2개월)", company: "㈜캐드뱅크산업디자인학원", role: "교육부서 주임 (최연소)", salary: "2,700만원", impact: "교육상담·회원관리·학원 프로그램 (최연소 주임 승진)" },
];
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
pnpm test --run tests/data/career.test.ts
```
Expected: PASS 4/4

- [ ] **Step 5: Commit**

```bash
git add lib/data/career.ts tests/data/career.test.ts
git commit -m "feat(data): 6 임팩트 스트림 + 17 회사 풀 (이력서 정확)"
```

### Task 1.3: Certifications 데이터 (3 비주얼 + 6 텍스트)

**Files:**
- Create: `lib/data/certifications.ts`
- Test: `tests/data/certifications.test.ts`

- [ ] **Step 1: 실패 테스트**

```ts
// tests/data/certifications.test.ts
import { describe, expect, it } from "vitest";
import { CERTS_VISUAL, CERTS_TEXT } from "@/lib/data/certifications";

describe("Certifications", () => {
  it("AI/마케팅 비주얼 카드 3개", () => {
    expect(CERTS_VISUAL).toHaveLength(3);
    expect(CERTS_VISUAL[0].name).toContain("AIPOT");
    expect(CERTS_VISUAL[1].name).toContain("검색광고");
    expect(CERTS_VISUAL[2].name).toContain("SNS광고");
  });

  it("나머지 텍스트 자격 6+", () => {
    expect(CERTS_TEXT.length).toBeGreaterThanOrEqual(6);
  });
});
```

- [ ] **Step 2: 데이터 구현**

```ts
// lib/data/certifications.ts
export type CertVisual = { name: string; year: string; issuer: string; code?: string; image: string; };
export type CertText = { name: string; year: string; issuer: string };

export const CERTS_VISUAL: CertVisual[] = [
  { name: "AIPOT 프롬프트엔지니어링 2급", year: "2025.02", issuer: "한국생산성본부 KPC", image: "/certifications/aipot.jpg" },
  { name: "검색광고마케터 1급", year: "2022.07", issuer: "한국정보통신진흥협회 KAIT", image: "/certifications/search-ad.jpg" },
  { name: "SNS광고마케터 1급", year: "2022.05", issuer: "한국정보통신진흥협회 KAIT", image: "/certifications/sns-ad.jpg" },
];

export const CERTS_TEXT: CertText[] = [
  { name: "CS강사 1급 (제18ASHA38180호)", year: "2018", issuer: "아샤서비스아카데미" },
  { name: "서비스 및 이미지컨설턴트 강사양성과정 100h", year: "2018", issuer: "아샤서비스아카데미" },
  { name: "스피치지도사 2급 · 리더십지도사 1급 · 고객관리지도사 1급", year: "2011", issuer: "한국경영인재개발원 KMRD" },
  { name: "사회복지사 2급 (제2-261521호)", year: "2010", issuer: "한국사회복지사협회/보건복지가족부" },
  { name: "워드프로세서 1급", year: "2012", issuer: "대한상공회의소" },
  { name: "컴퓨터활용능력 2급", year: "2010", issuer: "대한상공회의소" },
  { name: "1종보통운전면허", year: "2002", issuer: "경찰청" },
];
```

- [ ] **Step 3: 테스트 + 커밋**

```bash
pnpm test --run tests/data/certifications.test.ts
git add lib/data/certifications.ts tests/data/certifications.test.ts
git commit -m "feat(data): 자격증 3 비주얼 + 7 텍스트"
```

### Task 1.4: SaaS 데이터 (6 platform 풀 디테일 — 트렌디·풍부)

**Files:**
- Create: `lib/data/saas.ts`
- Test: `tests/data/saas.test.ts`

- [ ] **Step 1: 실패 테스트**

```ts
// tests/data/saas.test.ts
import { describe, expect, it } from "vitest";
import { SAAS_LIST } from "@/lib/data/saas";

describe("SaaS Data", () => {
  it("6 SaaS 정확한 순서 (사용자 명시)", () => {
    expect(SAAS_LIST).toHaveLength(6);
    expect(SAAS_LIST.map(s => s.slug)).toEqual(["tickpoint", "lumio", "os-agent", "mkt-automation", "propintel", "architect"]);
  });

  it("각 SaaS 5+ 능력 풀 (트렌디 룰)", () => {
    SAAS_LIST.forEach(s => expect(s.capabilities.length).toBeGreaterThanOrEqual(5));
  });

  it("약어 풀어서 표현 (VCP·CF 금지)", () => {
    const allText = JSON.stringify(SAAS_LIST);
    expect(allText).not.toMatch(/\bVCP\b/);
    expect(allText).not.toMatch(/\bCF 모드\b/);
    expect(allText).toContain("변동성 수축 패턴"); // VCP 풀어서
    expect(allText).toContain("광고 영상"); // CF 풀어서
  });
});
```

- [ ] **Step 2: 데이터 구현**

```ts
// lib/data/saas.ts
export type SaaSDetail = {
  slug: string;
  order: number;
  name: string;
  tagline: string;
  liveUrl: string;
  tone: "dark" | "light";
  toneNote: string;
  since?: string;
  capabilities: { title: string; description: string; image?: string }[];
  techStack: string[];
  metrics: { label: string; value: string }[];
  github?: string;
  folderImageCount: number;
  folderPath: string; // public/saas-folders/<slug>/
  capturedSlug: string; // public/captured/<slug>/...
  iconName: string;
  developer?: string;
  contactEmail?: string;
};

export const SAAS_LIST: SaaSDetail[] = [
  {
    slug: "tickpoint",
    order: 1,
    name: "Tickpoint",
    tagline: "한국 주식 트레이딩 인텔리전스 — KOSPI·KOSDAQ 2,769 종목을 멀티 LLM으로 통합 분석하는 라이브 SaaS",
    liveUrl: "https://tickpoint.co.kr/",
    tone: "dark",
    toneNote: "다크 + 빨강(하락)·초록(상승) 네온, TradingView 인상",
    since: "2026-04-24",
    capabilities: [
      { title: "변동성 수축 패턴 시그널 스크리너", description: "VCP(Volatility Contraction Pattern) 기반 주가 수축 종목 자동 감지 + 백테스트 KPI 9개 (924/204/175/37.1%/+1.42%/3.24/+55.17%/-12.21%)" },
      { title: "종합 분석 (멀티 LLM 오버레이)", description: "차트·캔들·볼륨 + 펀더멘털 + 9 기술지표 + LLM 전문가 오버레이 (Claude·GPT·Gemini·Perplexity)" },
      { title: "24 이슈 종목 자동 큐레이션 + 80+ 시그널", description: "매일 시장에서 가장 중요한 이슈 종목 24개 + 자동 시그널 80+개 큐레이션" },
      { title: "시장 심리 게이지", description: "공포·탐욕 인덱스 + 1H/24H/7D 뉴스 트렌드 + 핫 키워드 + 감정 분석" },
      { title: "자동매매 시스템", description: "관리자 권한 자동매매 + 신호 시뮬레이션 + 로또 6/45 보너스" },
      { title: "시장 현황 시각화", description: "섹터 히트맵 + 외인/기관/개인 차트 + 종목 검색 (2,769) + 개인 종목 (관심·보유)" },
    ],
    techStack: ["Next.js", "shadcn/ui", "Recharts", "TradingView", "Claude", "GPT", "Gemini", "Perplexity"],
    metrics: [
      { label: "종목 수", value: "2,769" },
      { label: "시그널", value: "80+" },
      { label: "백테스트 KPI", value: "9개" },
    ],
    folderImageCount: 15,
    folderPath: "/saas-folders/tickpoint",
    capturedSlug: "tickpoint",
    iconName: "TrendingUp",
    developer: "ls.Jung",
    contactEmail: "9843ohs@gmail.com",
  },
  {
    slug: "lumio",
    order: 2,
    name: "Lumio",
    tagline: "AI 광고 영상 자동 생성 스튜디오 — 소스 → 스토리 → 영상 → 합성 4단계 파이프라인",
    liveUrl: "https://lumio-video.vercel.app/",
    tone: "light",
    toneNote: "라이트 + 오렌지 액센트, shadcn/ui 카드/슬라이더",
    capabilities: [
      { title: "YouTube 영상 탐색 5방식", description: "키워드/URL/채널/카테고리/트렌드 — 조회수·좋아요·댓글·참여율 분석 후 등급 배지(Excellent/Good/Average/Low)" },
      { title: "씬 단위 스토리보드 자동 생성", description: "이미지 + TTS 나레이션 + 자막 + 트랜지션을 씬 별로 자동 구성" },
      { title: "이미지 모델 라우팅 4종", description: "Auto / Nano Banana Pro / GPT Image 2 / Flux Pro 2 자동 선택 또는 수동 지정" },
      { title: "영상·음악 모델 통합", description: "Veo 3.1 + Kling v3 Pro + ElevenLabs Music" },
      { title: "씬별 편집 풀세트", description: "트림·속도·밝기·대비·채도·페이드·Grayscale·Sepia·Blur·Vignette·Mirror·Negative·Vintage" },
      { title: "광고 영상 자동 생성 모드", description: "브랜드/제품 양식(메시지·타겟·컨셉·9:16/30s) → Gemini 3.1 Pro + Gemini Image + Kling v3 Pro + ElevenLabs Music 일괄 생성" },
    ],
    techStack: ["Next.js", "shadcn/ui", "Veo 3.1", "Kling v3 Pro", "Flux Pro 2", "Gemini Image", "ElevenLabs"],
    metrics: [{ label: "파이프라인", value: "4단계" }, { label: "이미지 모델", value: "4종" }],
    folderImageCount: 8,
    folderPath: "/saas-folders/lumio",
    capturedSlug: "lumio",
    iconName: "Film",
    developer: "ls.Jung",
  },
  {
    slug: "os-agent",
    order: 3,
    name: "OS Agent (Synapse)",
    tagline: "사내 AI OS — 12명 가상직원 + 20개 AI 에이전트가 부서별 협업하는 운영 시스템",
    liveUrl: "http://43.201.237.25:3010/",
    tone: "light",
    toneNote: "라이트 + 베이지·갈색 액센트",
    capabilities: [
      { title: "20개 AI 에이전트 라이브러리", description: "문서 관리자·카피라이터·데이터 툴킷·HR Agent·한글 문서 전문가·이미지 편집/생성/OCR·의도 분석기·맞춤 마케팅 보고서·PDF 분석/제작·프롬프트 엔지니어·스케줄 비서·SNS 분석기·텍스트 툴킷·영상 제작/도구함" },
      { title: "12명 가상 구성원 + 5개 부서", description: "경영/경영지원실/Agent 본부/Creative 본부/Marketing 본부 — 인사카드 (이름·직급·연락처·연봉·재직·기술스택)" },
      { title: "AI Agent 자연어 채팅 + 실시간 작업 처리", description: '예: "이번주 휴가자 누구야?" → 일정 조회 + 휴가 신청서 LR-2026-0042.pdf 자동 생성 + 팀 캘린더 등록 + 알림 4단계' },
      { title: "워크플로우 빌더 (노드 에디터)", description: "React Flow 기반 — HR Agent → 구성원 → 분기(자동 승인/조건/병렬) → AI Agent" },
      { title: "승인·결재", description: "휴가/반차/워크플로우 — 대기/승인/반려" },
      { title: "캘린더 + 작업 관리", description: "5월 캘린더 52개 일정 + 휴가 신청서 PDF·DOCX 다운로드" },
    ],
    techStack: ["Next.js", "shadcn/ui", "React Flow", "자체 LLM 라우팅"],
    metrics: [
      { label: "AI 에이전트", value: "20" },
      { label: "가상직원", value: "12" },
      { label: "부서", value: "5" },
    ],
    folderImageCount: 10,
    folderPath: "/saas-folders/os-agent",
    capturedSlug: "os-agent",
    iconName: "Network",
    developer: "ls.Jung (© 2026 Synapse)",
  },
  {
    slug: "mkt-automation",
    order: 4,
    name: "MKT Automation",
    tagline: "All-in-One 마케팅 OS — 네이버·구글·블로그·SNS·YouTube 단일 워크플로우",
    liveUrl: "https://frontend-three-smoky-68.vercel.app/",
    tone: "dark",
    toneNote: "다크 + 네온 그라디언트 블롭, 카테고리 컬러 코딩 (N·G·I·V·A)",
    since: "2025-12-19",
    capabilities: [
      { title: "8 통합 영역 / 50+ 자동화 도구 / 15+ AI 모델 / 6 SNS 분석", description: "네이버·구글·이미지·영상·광고 카테고리별 컬러 코딩 + 멀티 LLM 라우팅" },
      { title: "3 자동화 흐름", description: "블로그 콘텐츠 (키워드→경쟁분석→AI 글→이미지→자동 포스팅) / 비주얼 자산 (광고 카피·썸네일·상세페이지·카드뉴스·Cinema Studio) / 마케팅 인텔리전스 (SNS·키워드·딥리서치·전략 보고서·SEO)" },
      { title: "광고 카피 생성 3 모드", description: "집중/균형 (GPT+Claude+Gemini 15개) / 최대 다양성 (24개+) — 멀티 LLM" },
      { title: "YouTube 분석 6 모드 + SNS 분석", description: "인스타·페북·틱톡·스레드 + 감정 분석 + 통합" },
      { title: "자동 SEO 생성기", description: "URL/파일/텍스트 → 메타태그·사이트맵·robots.txt·웹사이트 분석" },
      { title: "48개+ 전문 AI 에이전트 협력 분석", description: "Multi-LLM (Claude+GPT+Gemini+Perplexity) 협력" },
    ],
    techStack: ["Next.js", "shadcn/ui", "Claude", "GPT", "Gemini", "Perplexity"],
    metrics: [
      { label: "자동화 도구", value: "50+" },
      { label: "AI 모델", value: "15+" },
      { label: "AI 에이전트", value: "48+" },
    ],
    github: "https://github.com/junginsu-make/AD_Copy",
    folderImageCount: 10,
    folderPath: "/saas-folders/mkt-automation",
    capturedSlug: "mkt-automation",
    iconName: "Megaphone",
    developer: "ls.Jung",
  },
  {
    slug: "propintel",
    order: 5,
    name: "PropIntel AI",
    tagline: "부동산 AI 어시스턴트 — 시장 + 등기 + 계약 사기 검증을 묶은 SaaS",
    liveUrl: "https://propintel-ai-beta.vercel.app/",
    tone: "light",
    toneNote: "라이트 + 초록 액센트, Recharts 인상",
    capabilities: [
      { title: "대시보드 — 실거래가 + 위험도 + AI 어시스턴트", description: "강남·서초·송파 라인차트 + 위험도 도넛 (안전 7/주의 2/위험 1) + 7일 활동 영역차트 + KPI (서울 가격지수 174.1 / 기준금리 2.50%)" },
      { title: "사기방지 계약 검증 5탭", description: "주소 진단 / 서류 확인 / 특약 검증 / 보증보험 / 입주 보호 — PDF·이미지 업로드 AI 분석 (세금체납·선순위 임차인·소유자 신분증·중개사 자격증·공제증서 자동 종합 위험도)" },
      { title: "공고 알림", description: "청약 2,713건 + 10개 지역" },
      { title: "시장 데이터 9탭", description: "아파트 매매·전월세 / 연립·다세대 / 청약 경쟁률 / 미분양 / 상권 / 가격지수 / 금리 / 종합" },
      { title: "등기 변경 이력 타임라인 + AI 분석", description: "소유권이전 / 근저당권 / 전세권 / 가처분 자동 추적" },
      { title: "부동산 뉴스 모니터링", description: "매일 08시 100건 분류 (시장동향 40 / 정비사업 20 / 정책규제 19 / 금리금융 12 / 건설사 9)" },
    ],
    techStack: ["Next.js", "shadcn/ui", "Recharts", "AI PDF 분석", "AI 이미지 분석"],
    metrics: [
      { label: "사기방지 탭", value: "5" },
      { label: "시장 데이터 탭", value: "9" },
      { label: "청약 알림", value: "2,713건" },
    ],
    folderImageCount: 7,
    folderPath: "/saas-folders/propintel",
    capturedSlug: "propintel",
    iconName: "Building",
    developer: "ls.Jung",
    contactEmail: "9843ohs@gmail.com",
  },
  {
    slug: "architect",
    order: 6,
    name: "아키텍처 시스템",
    tagline: "AI 비즈니스 진단 + 이중 출력 워크벤치 — 5단계 인터뷰만으로 제안서·PRD 동시 자동 생성",
    liveUrl: "https://architect-portfolio-lake.vercel.app/",
    tone: "dark",
    toneNote: "다크 + 보라 그라디언트(랜딩) / 라이트 + 컬러풀 결과 패널",
    capabilities: [
      { title: "5단계 비즈니스 진단", description: "비즈니스 배경 / 시스템 모델 / 업무 프로세스 / 기술 환경 / 성공 지표 KPI — 채팅 + 양식 양방향" },
      { title: "이중 출력", description: "단일 분석 → 비즈니스 제안서·ROI·일정표 (Claude) + PRD·LLD·API·DB·Frontend (Claude/Gemini) 동시 생성" },
      { title: "멀티모달 입력", description: "텍스트 / 문서 / 회의 녹음 → 분석" },
      { title: "결과물 5탭", description: "로드맵 (3개월 캘린더 + 27 스프린트 + GitHub·Terraform·Docker·CI/CD) / 아키텍처 (Mermaid 시퀀스) / 구현 / 문서 / UI 설계 (페이지 흐름도 + 와이어프레임 4종 + 디자인 토큰)" },
      { title: "클라이언트용 / 개발자용 뷰 토글", description: "한국어 / 영어 UI" },
    ],
    techStack: ["Vite", "React 19", "TypeScript 5.8", "Tailwind v4", "Zustand", "Dexie", "Mermaid.js", "jszip", "@google/genai", "@anthropic-ai/sdk"],
    metrics: [
      { label: "진단 단계", value: "5" },
      { label: "결과물 탭", value: "5" },
      { label: "동시 출력", value: "제안서+PRD" },
    ],
    github: "https://github.com/junginsu-make/Architect-Build",
    folderImageCount: 7,
    folderPath: "/saas-folders/architect",
    capturedSlug: "architect",
    iconName: "Workflow",
    developer: "ls.Jung",
  },
];
```

- [ ] **Step 3: 테스트 + 커밋**

```bash
pnpm test --run tests/data/saas.test.ts
git add lib/data/saas.ts tests/data/saas.test.ts
git commit -m "feat(data): 6 SaaS 풀 디테일 (트렌디 풍부, 약어 풀어서)"
```

### Task 1.5: Scenarios 데이터 (4 핵심 + 81 보조)

**Files:**
- Create: `lib/data/scenarios.ts`
- Test: `tests/data/scenarios.test.ts`

- [ ] **Step 1: 실패 테스트**

```ts
import { describe, expect, it } from "vitest";
import { CORE_SCENARIOS, INVENTORY_GROUPS } from "@/lib/data/scenarios";

describe("Make.com Scenarios", () => {
  it("4 핵심 시나리오 정확", () => {
    expect(CORE_SCENARIOS).toHaveLength(4);
    expect(CORE_SCENARIOS[0].title).toContain("정부지원사업");
    expect(CORE_SCENARIOS[1].title).toContain("10X");
    expect(CORE_SCENARIOS[2].title).toContain("계약서");
    expect(CORE_SCENARIOS[3].title).toContain("뉴스레터");
  });

  it("81 인벤토리 6 제품군 보조 라벨", () => {
    expect(INVENTORY_GROUPS).toHaveLength(6);
    expect(INVENTORY_GROUPS.every(g => g.isMain === false)).toBe(true);
  });

  it("서울법무법인 메인에 노출 X (클라이언트 작업)", () => {
    const allText = JSON.stringify(CORE_SCENARIOS);
    expect(allText).not.toContain("서울법무법인");
  });
});
```

- [ ] **Step 2: 구현**

```ts
// lib/data/scenarios.ts
export type CoreScenario = {
  number: number;
  title: string;
  flow: string[];
  metric: string;
  screenshots: string[];
};

export type InventoryGroup = {
  name: string;
  systemRange: string;
  count: number;
  isMain: false; // 보조 라벨 (메인 X)
};

export const CORE_SCENARIOS: CoreScenario[] = [
  {
    number: 1,
    title: "정부지원사업 크롤링·사업계획서 자동화",
    flow: ["Webhook", "Airtable", "CloudConvert/PDF.co", "OpenAI 6+ 분기", "Google Docs", "Airtable 상태 업데이트"],
    metric: "24+ 공고 누적",
    screenshots: ["/automation/scenario-1-flow.jpg", "/automation/scenario-1-airtable.jpg"],
  },
  {
    number: 2,
    title: "10X 콘텐츠 자동화",
    flow: ["Webhook", "Airtable 10X Content", "OpenAI 1차 (4-5개)", "HTTP/Sleep", "Iterator", "OpenAI 2차", "Wordpress 5채널 분기", "GDrive", "Airtable"],
    metric: "5개 WP 동시 / Published Blog 6+ / Deep Dive 18+ 행",
    screenshots: ["/automation/scenario-2-flow.jpg", "/automation/scenario-2-published.jpg", "/automation/scenario-2-deepdive.jpg"],
  },
  {
    number: 3,
    title: "계약서 관리 시스템 + GA4 분석 자동화",
    flow: ["Drive Watch Folder", "Drive Download", "PDF Convert", "OpenAI Generate", "JSON Parse", "Airtable Create", "폴더 ID 서치", "Router(폴더 존재/신규/벤도 신규)", "계약서 url 업데이트"],
    metric: "광고주 폴더 자동 분류",
    screenshots: ["/automation/scenario-3-flow.jpg"],
  },
  {
    number: 4,
    title: "뉴스레터 또는 웹사이트 크롤링 후 콘텐츠 생성·업로드 자동화",
    flow: ["Webhook", "Tools", "Airtable", "SerpAPI 5+ 카테고리", "데이터 정제", "Iterator+Router 5+ 분기", "OpenAI 통합 분석", "Airtable"],
    metric: "Grid view 25 키워드 / 상세 47+ 콘텐츠",
    screenshots: ["/automation/scenario-4-flow.jpg", "/automation/scenario-4-grid.jpg", "/automation/scenario-4-detail.jpg"],
  },
];

export const INVENTORY_GROUPS: InventoryGroup[] = [
  { name: "AI 검색·SEO 콘텐츠 엔진", systemRange: "01-17, 63-66, 81", count: 23, isMain: false },
  { name: "버티컬 업무 자동화 (정부지원·법무·금융)", systemRange: "18-36", count: 19, isMain: false },
  { name: "SNS·숏폼·멀티미디어", systemRange: "37-42, 52-60, 75", count: 16, isMain: false },
  { name: "뉴스·지식 큐레이션", systemRange: "43-51", count: 9, isMain: false },
  { name: "데이터 수집·분석 인프라", systemRange: "02, 06-07, 61-70, 76-79", count: 15, isMain: false },
  { name: "마케팅·광고·운영 자동화", systemRange: "71-74, 80", count: 5, isMain: false },
];

export const TIMELINE_2024_2025 = [
  { period: "2024.10", phase: "기반 구축", note: "Make.com 실습 / Notion+Claude+Flux / RSS+IG / YouTube 채널 분석" },
  { period: "2024.11", phase: "확장 성장", note: "정부지원사업 / Midjourney 3단계 / Suno / AI 쇼츠" },
  { period: "2024.12", phase: "심화 발전", note: "구글 키워드 블로그 / 정부지원 사업계획서 / 증권 리포트" },
  { period: "2025.01", phase: "통합·고도화", note: "퍼플렉시티-WP / 10X 콘텐츠" },
  { period: "2025.02-03", phase: "전문화", note: "YouTube to Blog / 고객사 마케팅 전략 / 온페이지 SEO" },
  { period: "2025.04", phase: "클라이언트 납품", note: "5채널 자동화 / 연예인 뉴스" },
  { period: "2025.05", phase: "최신 기술 통합", note: "AI FAKE 릴스 NEWS CMS / Vidu/Kling / Castsome" },
  { period: "2025.06-08", phase: "운영·협업 자동화", note: "Apify 통합 / Pltt AI News 3채널 / Google Chat 알림" },
];
```

- [ ] **Step 3: 테스트 + 커밋**

```bash
pnpm test --run tests/data/scenarios.test.ts
git add lib/data/scenarios.ts tests/data/scenarios.test.ts
git commit -m "feat(data): 4 핵심 시나리오 + 81 인벤토리 6 제품군 (보조)"
```

### Task 1.6: Teaching 데이터 (3 Tier)

**Files:**
- Create: `lib/data/teaching.ts`
- Test: `tests/data/teaching.test.ts`

- [ ] **Step 1: 실패 테스트**

```ts
import { describe, expect, it } from "vitest";
import { TIER1, TIER2, TIER3 } from "@/lib/data/teaching";

describe("Teaching Tiers", () => {
  it("Tier 1 정부·대학·AI 6 카드", () => {
    expect(TIER1).toHaveLength(6);
    expect(TIER1.find(t => t.name.includes("KOICA"))).toBeDefined();
  });
  it("Tier 2 협회·기업 8 카드", () => expect(TIER2).toHaveLength(8));
  it("Tier 3 개인 텍스트 15+", () => expect(TIER3.length).toBeGreaterThanOrEqual(15));
});
```

- [ ] **Step 2: 구현**

```ts
// lib/data/teaching.ts
export type Tier1Card = { name: string; category: "정부" | "공공" | "대학" | "AI 교육"; image: string; desc?: string };
export type Tier2Card = { name: string; image?: string };

export const TIER1: Tier1Card[] = [
  { name: "국제협력단 KOICA 해외봉사단", category: "정부", image: "/photos/teaching/IMG_1187.jpg", desc: "고향방문단 국내 교육" },
  { name: "한국산림복지진흥원 AI 교육", category: "AI 교육", image: "/photos/teaching/p1070868.jpg", desc: "정부 산하 기관" },
  { name: "제2서울핀테크랩 AI 교육", category: "AI 교육", image: "/photos/teaching/p1070-fintech.jpg", desc: "서울시 공공 핀테크 인프라" },
  { name: "성동청년창업이룸센터 AI 교육", category: "AI 교육", image: "/photos/teaching/seongdong.jpg", desc: "성동구 공공 창업 지원" },
  { name: "고려대학교기술지주 AI 교육", category: "AI 교육", image: "/photos/teaching/korea-univ.jpg", desc: "대학 산학협력" },
  { name: "청운대학교", category: "대학", image: "/photos/teaching/dsc01363.jpg", desc: "호텔 연회장 60명+ 심층 세미나" },
];

export const TIER2: Tier2Card[] = [
  { name: "메가스터디 더조은컴퓨터학원" },
  { name: "동작구 소상공인 연합회" },
  { name: "서대문구 학원협회 50여 곳" },
  { name: "KCT 보습학원" },
  { name: "헬로우뮤지엄 미술관" },
  { name: "전국 태권도장·합기도장 관장 300여명" },
  { name: "세무사 모임" },
  { name: "인테리어 디자이너" },
];

export const TIER3: string[] = [
  "영업 직원", "개인 블로그모임", "작가", "에이스 인쇄·제본", "명품시계 AS센터",
  "1인 네일샵", "의정부 컴퓨터 AS센터", "Y-GYM", "음악학원", "아샤 아카데미",
  "1인샵 윤정문 안마원", "대전 이사업체", "경기도 청소업체",
  "수원 삼성경매공인 부동산", "퍼스트 아카데미",
];

// 강의 사진 자동 롤링 캐러셀 (56장 풀 — capture script가 public/photos/teaching/ 채움)
// 캐러셀 컴포넌트가 디렉토리 스캔 (또는 manifest)
export const TEACHING_HERO = "/photos/teaching/KakaoTalk_20200102_190424548_01.jpg";
```

- [ ] **Step 3: 테스트 + 커밋**

```bash
pnpm test --run tests/data/teaching.test.ts
git add lib/data/teaching.ts tests/data/teaching.test.ts
git commit -m "feat(data): 강의 3 Tier (정부·대학·AI 6 / 협회·기업 8 / 개인 텍스트 15)"
```

### Task 1.7: 나머지 데이터 — Home, Marketing, Content Refs, GitHub

**Files:**
- Create: `lib/data/home.ts`, `lib/data/marketing.ts`, `lib/data/content-refs.ts`, `lib/data/github.ts`
- Test: `tests/data/{home,marketing,github}.test.ts`

- [ ] **Step 1: home.ts**

```ts
// lib/data/home.ts
export const MANIFESTO = "AI 시대를 만난 17년 마케터가, 풀사이클 빌더로 다시 태어났다";

export const HERO_META = {
  name: "정인수",
  birth: "1983",
  city: "서울",
  role: "Palette ㈜ Agent 본부 팀장",
};

export const COUNTERS = [
  { value: "17", suffix: "년", label: "Career" },
  { value: "84", suffix: "", label: "GitHub Repos" },
  { value: "4", suffix: "", label: "Make.com Core" },
  { value: "6", suffix: "", label: "Live SaaS" },
];

export const TECH_BADGES = [
  "Make.com", "Claude", "GPT", "Gemini", "Perplexity", "n8n", "MCP", "React/Next.js", "Supabase",
];

export const CTA_QUOTE = "커피 한 잔이면 됩니다";
export const CONTACT_EMAIL = "9843ohs@gmail.com";
```

- [ ] **Step 2: marketing.ts (AI PL + Content Op + Capsule PM)**

```ts
// lib/data/marketing.ts
export const AI_PL_CLIENTS = ["호반그룹", "서울법무법인", "아주그룹", "Palette OS Agent"];
export const AI_CONTENT_OP_CLIENTS = [
  "성동청년이룸창업지원센터",
  "고려대학교기술지주",
  "제2서울핀테크랩",
  "서울도시철도엔지니어링",
  "시스트란",
  "모두솔루션",
  "리부트라이프",
];
export const CAPSULE_PM_PROJECTS = [
  { name: "농림축산식품부", role: "소셜미디어·마케팅 홍보 영역 PM" },
  { name: "조달청", role: "소셜미디어·마케팅 홍보 영역 PM" },
  { name: "한국벤처투자", role: "과업 총괄 PM" },
  { name: "창업진흥원", role: "2023년도 과업 총괄 PM" },
];
export const CAPSULE_PM_OTHERS = "+14건";

export const AD_CHANNELS = {
  owned: ["Blog", "SNS", "YouTube"],
  paid: ["Naver", "Kakao", "Google", "Instagram", "Facebook", "GDN"],
};

export const COMPANY_AD_BUDGET = [
  { company: "퍼포먼스디자인", budget: "연 40~60억", note: "티몬 공식 광고대행 9개월" },
  { company: "퍼스트 아카데미", budget: "연 20억+", note: "전국 30지점 1년 3개월" },
];
```

- [ ] **Step 3: content-refs.ts (15+ url placeholder, capture 스크립트가 채움)**

```ts
// lib/data/content-refs.ts
export type ContentRef = {
  url: string;
  title: string;
  publisher: string;
  date?: string;
  category: "blog" | "instagram" | "youtube" | "notion" | "external";
  capturedImage?: string; // public/captured/<slug>/desktop.jpg
  embedType?: "iframe" | "screenshot";
};

// scripts/capture-urls.ts 가 자료 폴더 콘텐츠 레퍼런스.txt 파싱 후 이 배열 채움
// (런타임에는 captured-meta.json 사용)
export const CONTENT_REFS_PLACEHOLDER: ContentRef[] = [];
```

- [ ] **Step 4: github.ts**

```ts
// lib/data/github.ts
export const GITHUB_TOTAL = 84;
export const GITHUB_PROFILE = "https://github.com/junginsu-make";

export const GITHUB_CATEGORIES = [
  { id: "A", name: "대표 시스템 (AI Agent / 마케팅 / 금융 / 영상 / 이미지 / 문서)", count: 35, examples: ["synapse-platform", "propintel-ai", "stock-training", "palette-hoban-develop", "cinema_AI", "MARKETING_SEO_AUTOMATION", "AD_Copy", "Image_Agent", "legal-agent"] },
  { id: "B", name: "Claude Code 생태계", count: 11, examples: ["claude-forge", "my-claude-code-setting", "claude-code-skills", "frontend-toolkit", "spec-kit", "claw-code"] },
  { id: "C", name: "Frontend Stack 큐레이션 (학습용 fork)", count: 16, examples: ["shadcn-landing-page", "next-shadcn-dashboard-starter", "SaaS-Boilerplate", "zustand", "motion", "query", "recharts"] },
  { id: "D", name: "외부 도구·학습 fork", count: 6, examples: ["hwp-open-source", "NAVER_NESS", "spider", "python-bithumb", "Qwen-Image-Layered"] },
  { id: "E", name: "Misc/부속", count: 6, examples: ["2026-New-Year-s-Fortune", "suno-api", "youtube-studio", "replit", "Architect-Build", "LLM-Model"] },
  { id: "NEW", name: "신규 추가 (gh 직접 보강 필요)", count: 3, examples: [] },
];

export const GOLDEN_PRINCIPLES = [
  "Immutability", "Secrets in Env", "TDD (RED-GREEN-IMPROVE)", "Conclusion First",
  "Small Files (800·50 라인)", "Validate at Boundaries", "Explain with Analogies",
  "Context 50% Rule", "HARD-GATE: No Coding Without Design",
  "Evidence-Based Completion", "SDD Review Enforcement", "Surgical Changes",
];
```

- [ ] **Step 5: 모든 데이터 import + 테스트 통과 확인**

```bash
pnpm test --run
```
Expected: PASS all (career + certifications + saas + scenarios + teaching)

- [ ] **Step 6: Commit**

```bash
git add lib/data tests/data
git commit -m "feat(data): 나머지 home/marketing/content/github 데이터 + 7+ 클라이언트"
```

---

## Phase 2 — Asset Pipeline (자료 복사 + Playwright 캡처 + sharp 최적화)

### Task 2.1: 자료 폴더에서 public으로 자료 복사

**Files:** Modify `public/photos`, `public/certifications`, `public/automation`, `public/saas-folders/{6}`

- [ ] **Step 1: 복사 스크립트 작성**

`scripts/copy-assets.sh`:

```bash
#!/bin/bash
set -euo pipefail

SRC="/home/a20616050/projects/A/포토폴리오-260503"
DST="/home/a20616050/projects/A/junginsu-portfolio-v3/public"

# 새 고화질 증명사진
cp "$SRC/정인수 증명사진 고화질.png" "$DST/photos/profile-hero.png"

# 강의 사진 56장 + 강의경력 PDF
cp -r "$SRC/강의 사진/"*.{jpg,JPG,png,PNG} "$DST/photos/teaching/" 2>/dev/null || true

# 자격증 9 + PDF
cp -r "$SRC/자격증 이미지/"*.{jpg,JPG,png,PNG,pdf} "$DST/certifications/" 2>/dev/null || true

# make.com 11 + PDF 2
cp -r "$SRC/make.com/"*.{jpg,JPG,JPEG,pdf} "$DST/automation/" 2>/dev/null || true

# 6 SaaS 폴더 (각 슬러그별)
for slug in tickpoint lumio "OS Agent" "MKT Automation" propintel "아키텍처 시스템"; do
  if [ "$slug" = "OS Agent" ]; then dst_slug=os-agent
  elif [ "$slug" = "MKT Automation" ]; then dst_slug=mkt-automation
  elif [ "$slug" = "아키텍처 시스템" ]; then dst_slug=architect
  else dst_slug="$slug"
  fi
  src_dir="$SRC/github-vibe coding/$slug"
  if [ -d "$src_dir" ]; then
    cp -r "$src_dir/"*.{jpg,JPG,png,PNG} "$DST/saas-folders/$dst_slug/" 2>/dev/null || true
  fi
done

# 콘텐츠 레퍼런스
mkdir -p "$DST/content-refs"
cp -r "$SRC/github-vibe coding/MKT Automation/콘텐츠 레퍼런스/"* "$DST/content-refs/" 2>/dev/null || true

# 이력서 PDF
cp "$SRC/정인수 이력서.pdf" "$DST/resume.pdf"

echo "Asset copy 완료"
ls "$DST/photos/teaching/" | wc -l
ls "$DST/saas-folders/tickpoint/" | wc -l
```

```bash
chmod +x scripts/copy-assets.sh
bash scripts/copy-assets.sh
```

- [ ] **Step 2: 결과 확인**

```bash
ls public/photos/teaching/ | wc -l    # ~56
ls public/certifications/ | wc -l    # ~10
ls public/automation/ | wc -l        # ~13
ls public/saas-folders/tickpoint/ | wc -l    # ~15
```

- [ ] **Step 3: .gitignore 업데이트 (대용량 자료 git X)**

```bash
echo "
# 자료 (capture script가 만들거나 copy script가 채움)
public/photos/teaching/
public/saas-folders/
public/captured/
public/cropped/
public/automation/
" >> .gitignore
```

- [ ] **Step 4: Commit**

```bash
git add scripts/copy-assets.sh .gitignore public/photos/profile-hero.png public/certifications public/content-refs public/resume.pdf
git commit -m "chore(assets): copy materials from 포토폴리오-260503 + .gitignore large dirs"
```

### Task 2.2: 이미지 최적화 스크립트 (sharp webp/avif)

**Files:** Create `scripts/optimize-images.ts`

- [ ] **Step 1: 스크립트 작성**

```ts
// scripts/optimize-images.ts
import sharp from "sharp";
import { readdir, stat, mkdir } from "fs/promises";
import { join, extname, basename, dirname } from "path";

const PUBLIC_DIRS = [
  "public/photos/teaching",
  "public/saas-folders/tickpoint",
  "public/saas-folders/lumio",
  "public/saas-folders/os-agent",
  "public/saas-folders/mkt-automation",
  "public/saas-folders/propintel",
  "public/saas-folders/architect",
  "public/automation",
  "public/captured",
];

async function* walk(dir: string): AsyncGenerator<string> {
  for (const entry of await readdir(dir, { withFileTypes: true })) {
    const path = join(dir, entry.name);
    if (entry.isDirectory()) yield* walk(path);
    else yield path;
  }
}

async function optimize(file: string) {
  const ext = extname(file).toLowerCase();
  if (![".jpg", ".jpeg", ".png"].includes(ext)) return;
  const dir = dirname(file);
  const name = basename(file, ext);
  const webpOut = join(dir, `${name}.webp`);
  await sharp(file).resize({ width: 2000, withoutEnlargement: true }).webp({ quality: 80 }).toFile(webpOut);
  const avifOut = join(dir, `${name}.avif`);
  await sharp(file).resize({ width: 2000, withoutEnlargement: true }).avif({ quality: 65 }).toFile(avifOut);
  console.log(`✓ ${file}`);
}

(async () => {
  for (const root of PUBLIC_DIRS) {
    try {
      for await (const file of walk(root)) await optimize(file);
    } catch {}
  }
})();
```

- [ ] **Step 2: 실행 (자료 복사 후)**

```bash
pnpm tsx scripts/optimize-images.ts
```

- [ ] **Step 3: 결과 확인 + 크기 비교**

```bash
du -sh public/photos/teaching/
ls public/photos/teaching/*.webp | head -5
```
Expected: webp + avif 파일 생성, 총 크기 절반 이하

- [ ] **Step 4: Commit**

```bash
git add scripts/optimize-images.ts package.json
git commit -m "feat(assets): sharp 이미지 최적화 스크립트 (webp + avif, max 2000px)"
```

### Task 2.3: Playwright 캡처 스크립트 (50~70 URL 일괄)

**Files:** Create `scripts/url-list.json`, `scripts/capture-urls.ts`

- [ ] **Step 1: url-list.json 작성**

```json
[
  { "slug": "tickpoint/home", "url": "https://tickpoint.co.kr/" },
  { "slug": "tickpoint/screener", "url": "https://tickpoint.co.kr/screener" },
  { "slug": "lumio/home", "url": "https://lumio-video.vercel.app/" },
  { "slug": "os-agent/home", "url": "http://43.201.237.25:3010/" },
  { "slug": "mkt-automation/home", "url": "https://frontend-three-smoky-68.vercel.app/" },
  { "slug": "propintel/home", "url": "https://propintel-ai-beta.vercel.app/" },
  { "slug": "architect/home", "url": "https://architect-portfolio-lake.vercel.app/" },
  { "slug": "github/profile", "url": "https://github.com/junginsu-make" },
  { "slug": "blog/sbcyberpass", "url": "https://blog.naver.com/sbcyberpass" }
]
```

> 콘텐츠 레퍼런스 url은 `public/content-refs/콘텐츠 레퍼런스.txt`에서 자동 파싱 (다음 단계에서 추가)

- [ ] **Step 2: capture-urls.ts 작성**

```ts
// scripts/capture-urls.ts
import { chromium, type Browser } from "playwright";
import { readFile, writeFile, mkdir } from "fs/promises";
import { dirname, join } from "path";

type Target = { slug: string; url: string };
type CapturedMeta = { slug: string; url: string; capturedAt: string; ok: boolean; error?: string };

const VIEWPORTS = [
  { name: "desktop", width: 1440, height: 900 },
  { name: "mobile", width: 375, height: 812 },
];

async function captureOne(browser: Browser, target: Target, viewport: typeof VIEWPORTS[0]): Promise<CapturedMeta> {
  const ctx = await browser.newContext({ viewport: { width: viewport.width, height: viewport.height } });
  const page = await ctx.newPage();
  const outPath = join("public/captured", `${target.slug}/${viewport.name}.jpg`);
  await mkdir(dirname(outPath), { recursive: true });
  try {
    await page.goto(target.url, { waitUntil: "networkidle", timeout: 30000 });
    await page.waitForTimeout(2000);
    await page.screenshot({ path: outPath, fullPage: true, type: "jpeg", quality: 80 });
    await ctx.close();
    return { slug: `${target.slug}/${viewport.name}`, url: target.url, capturedAt: new Date().toISOString(), ok: true };
  } catch (e) {
    await ctx.close().catch(() => {});
    return { slug: `${target.slug}/${viewport.name}`, url: target.url, capturedAt: new Date().toISOString(), ok: false, error: String(e) };
  }
}

(async () => {
  const targets: Target[] = JSON.parse(await readFile("scripts/url-list.json", "utf-8"));
  const browser = await chromium.launch();
  const results: CapturedMeta[] = [];
  for (const target of targets) {
    for (const vp of VIEWPORTS) {
      console.log(`Capturing ${target.slug} @ ${vp.name}...`);
      results.push(await captureOne(browser, target, vp));
    }
  }
  await browser.close();
  await writeFile("lib/captured-meta.json", JSON.stringify(results, null, 2));
  const ok = results.filter(r => r.ok).length;
  const fail = results.length - ok;
  console.log(`\n✓ ${ok} / ✗ ${fail}`);
})();
```

- [ ] **Step 3: 실행 (60~90분 예상, background로)**

```bash
pnpm tsx scripts/capture-urls.ts 2>&1 | tee capture.log
```

> 사용자 확인: 시간 소요 큼. 다른 작업 병행 진행 가능. OS Agent IP가 다운이면 실패 OK.

- [ ] **Step 4: 결과 확인**

```bash
cat lib/captured-meta.json | grep '"ok": true' | wc -l   # 성공 캡처 수
ls public/captured/tickpoint/    # desktop.jpg + mobile.jpg
```

- [ ] **Step 5: Commit**

```bash
git add scripts/url-list.json scripts/capture-urls.ts lib/captured-meta.json
git commit -m "feat(capture): Playwright 일괄 캡처 스크립트 (데스크탑+모바일 fullPage)"
```

---

## Phase 3 — 모션 Primitives

### Task 3.1: Lenis 스무스 스크롤 provider

**Files:** Create `components/motion/lenis-provider.tsx`

- [ ] **Step 1: 작성**

```tsx
"use client";
import { useEffect } from "react";
import Lenis from "lenis";

export function LenisProvider({ children }: { children: React.ReactNode }) {
  useEffect(() => {
    const lenis = new Lenis({ lerp: 0.1, smoothWheel: true });
    function raf(time: number) { lenis.raf(time); requestAnimationFrame(raf); }
    requestAnimationFrame(raf);
    return () => lenis.destroy();
  }, []);
  return <>{children}</>;
}
```

- [ ] **Step 2: app/layout.tsx에 wrap**

```tsx
import { LenisProvider } from "@/components/motion/lenis-provider";
// ... <body><LenisProvider>{children}</LenisProvider></body>
```

- [ ] **Step 3: 검증 + 커밋**

```bash
pnpm dev
# 브라우저에서 스크롤 시 부드러운 관성 확인
git add components/motion/lenis-provider.tsx app/layout.tsx
git commit -m "feat(motion): Lenis 스무스 스크롤 provider"
```

### Task 3.2: GSAP ScrollTrigger 등록 + 핀 섹션 wrapper

**Files:** Create `lib/motion.ts`, `components/motion/pin-section.tsx`

- [ ] **Step 1: lib/motion.ts**

```ts
"use client";
import gsap from "gsap";
import { ScrollTrigger } from "gsap/ScrollTrigger";

if (typeof window !== "undefined") gsap.registerPlugin(ScrollTrigger);
export { gsap, ScrollTrigger };
```

- [ ] **Step 2: pin-section.tsx**

```tsx
"use client";
import { useRef, useEffect } from "react";
import { gsap, ScrollTrigger } from "@/lib/motion";

export function PinSection({ children, height = "100vh", className = "" }: { children: React.ReactNode; height?: string; className?: string }) {
  const ref = useRef<HTMLDivElement>(null);
  useEffect(() => {
    if (!ref.current) return;
    const el = ref.current;
    const trigger = ScrollTrigger.create({ trigger: el, start: "top top", end: `+=${parseInt(height)}`, pin: true, pinSpacing: true });
    return () => trigger.kill();
  }, [height]);
  return <section ref={ref} className={className} style={{ minHeight: height }}>{children}</section>;
}
```

- [ ] **Step 3: 검증 + 커밋**

```bash
git add lib/motion.ts components/motion/pin-section.tsx
git commit -m "feat(motion): GSAP ScrollTrigger 등록 + PinSection wrapper"
```

### Task 3.3: 마그네틱 버튼 hook + 컴포넌트

**Files:** Create `components/motion/magnetic.tsx`

- [ ] **Step 1: 작성**

```tsx
"use client";
import { useRef, useState } from "react";
import { motion, useSpring } from "framer-motion";

export function Magnetic({ children, strength = 0.4, className = "" }: { children: React.ReactNode; strength?: number; className?: string }) {
  const ref = useRef<HTMLDivElement>(null);
  const x = useSpring(0, { stiffness: 150, damping: 15 });
  const y = useSpring(0, { stiffness: 150, damping: 15 });

  function handleMove(e: React.MouseEvent) {
    if (!ref.current) return;
    const rect = ref.current.getBoundingClientRect();
    x.set(((e.clientX - rect.left) / rect.width - 0.5) * rect.width * strength);
    y.set(((e.clientY - rect.top) / rect.height - 0.5) * rect.height * strength);
  }
  function handleLeave() { x.set(0); y.set(0); }

  return (
    <motion.div ref={ref} onMouseMove={handleMove} onMouseLeave={handleLeave} style={{ x, y }} className={className}>
      {children}
    </motion.div>
  );
}
```

- [ ] **Step 2: 검증 + 커밋**

```bash
git add components/motion/magnetic.tsx
git commit -m "feat(motion): Magnetic wrapper (framer-motion spring)"
```

### Task 3.4: Counter (count up)

**Files:** Create `components/motion/counter.tsx`

```tsx
"use client";
import { useEffect, useRef } from "react";
import { animate, useInView, useMotionValue, useTransform, motion } from "framer-motion";

export function Counter({ to, suffix = "", duration = 1.6 }: { to: number; suffix?: string; duration?: number }) {
  const ref = useRef<HTMLSpanElement>(null);
  const inView = useInView(ref, { once: true, margin: "-30% 0px" });
  const mv = useMotionValue(0);
  const display = useTransform(mv, n => Math.round(n).toLocaleString() + suffix);

  useEffect(() => {
    if (inView) animate(mv, to, { duration, ease: [0.6, 0.05, 0.3, 0.95] });
  }, [inView, mv, to, duration]);

  return <motion.span ref={ref}>{display}</motion.span>;
}
```

- [ ] **Step 1: 작성 + 커밋**

```bash
git add components/motion/counter.tsx
git commit -m "feat(motion): Counter (count up with framer-motion)"
```

### Task 3.5: KineticText (Fraunces axes scrub)

**Files:** Create `components/motion/kinetic-text.tsx`

```tsx
"use client";
import { useRef, useEffect } from "react";
import { gsap } from "@/lib/motion";

export function KineticText({ children, className = "" }: { children: string; className?: string }) {
  const ref = useRef<HTMLSpanElement>(null);

  useEffect(() => {
    if (!ref.current) return;
    const el = ref.current;
    const handleMove = (e: MouseEvent) => {
      const rect = el.getBoundingClientRect();
      const cx = (e.clientX - rect.left) / rect.width;
      const cy = (e.clientY - rect.top) / rect.height;
      gsap.to(el, {
        // Fraunces SOFT (0~100) + WONK (0~1)
        '--soft': 50 + cx * 50,
        '--wonk': cy.toFixed(2),
        duration: 0.6,
        ease: "power2.out",
      });
    };
    window.addEventListener("mousemove", handleMove);
    return () => window.removeEventListener("mousemove", handleMove);
  }, []);

  return (
    <span ref={ref} className={`kinetic-text ${className}`} style={{ fontVariationSettings: "'SOFT' var(--soft, 50), 'WONK' var(--wonk, 0)" }}>
      {children}
    </span>
  );
}
```

- [ ] **Step 1: 작성 + 커밋**

```bash
git add components/motion/kinetic-text.tsx
git commit -m "feat(motion): KineticText (Fraunces SOFT/WONK axes follow mouse)"
```

### Task 3.6: ColorSweep 페이지 전환 (View Transitions API)

**Files:** Create `components/motion/color-sweep.tsx`, modify `app/globals.css`

- [ ] **Step 1: globals.css 추가**

`app/globals.css` 끝에:

```css
/* View Transitions — 컬러 스윕 */
@view-transition { navigation: auto; }

::view-transition-old(root) {
  animation: sweep-out 320ms cubic-bezier(0.85, 0, 0.15, 1) forwards;
}
::view-transition-new(root) {
  animation: sweep-in 320ms cubic-bezier(0.85, 0, 0.15, 1) forwards;
}
@keyframes sweep-out { to { transform: translateX(-100%); } }
@keyframes sweep-in { from { transform: translateX(100%); } }

/* 오렌지 패널 (브라우저 지원 시 자동 노출) */
::view-transition-group(root) {
  background: var(--color-orange);
}
```

- [ ] **Step 2: 폴백 컴포넌트 (Firefox 등 미지원)**

`components/motion/color-sweep.tsx`:

```tsx
"use client";
import Link from "next/link";
import { useRouter } from "next/navigation";

export function SweepLink({ href, children, className = "" }: { href: string; children: React.ReactNode; className?: string }) {
  const router = useRouter();
  function handleClick(e: React.MouseEvent) {
    e.preventDefault();
    if ('startViewTransition' in document) {
      (document as any).startViewTransition(() => router.push(href));
    } else {
      router.push(href);
    }
  }
  return <Link href={href} onClick={handleClick} className={className}>{children}</Link>;
}
```

- [ ] **Step 3: 커밋**

```bash
git add app/globals.css components/motion/color-sweep.tsx
git commit -m "feat(motion): View Transitions 컬러 스윕 페이지 전환 + SweepLink 폴백"
```

---

## Phase 4 — Theme + Layout

### Task 4.1: 테마 provider (시스템 prefers-color-scheme + localStorage)

**Files:** Create `lib/theme.tsx`

- [ ] **Step 1: 작성**

```tsx
"use client";
import { createContext, useContext, useEffect, useState } from "react";

type Theme = "light" | "dark";
const ThemeContext = createContext<{ theme: Theme; setTheme: (t: Theme) => void } | null>(null);

export function ThemeProvider({ children }: { children: React.ReactNode }) {
  const [theme, setTheme] = useState<Theme>("light");

  useEffect(() => {
    const saved = localStorage.getItem("theme") as Theme | null;
    const sys = window.matchMedia("(prefers-color-scheme: dark)").matches ? "dark" : "light";
    setTheme(saved ?? sys);
  }, []);

  useEffect(() => {
    document.documentElement.dataset.theme = theme;
    document.documentElement.classList.toggle("dark", theme === "dark");
    localStorage.setItem("theme", theme);
  }, [theme]);

  return <ThemeContext.Provider value={{ theme, setTheme }}>{children}</ThemeContext.Provider>;
}

export function useTheme() {
  const ctx = useContext(ThemeContext);
  if (!ctx) throw new Error("useTheme must be used within ThemeProvider");
  return ctx;
}
```

- [ ] **Step 2: 커밋**

```bash
git add lib/theme.tsx
git commit -m "feat(theme): ThemeProvider with prefers-color-scheme + localStorage"
```

### Task 4.2: 다크/라이트 토글 (Sun/Moon Lucide + 마그네틱)

**Files:** Create `components/layout/theme-toggle.tsx`

```tsx
"use client";
import { Sun, Moon } from "lucide-react";
import { Magnetic } from "@/components/motion/magnetic";
import { useTheme } from "@/lib/theme";

export function ThemeToggle() {
  const { theme, setTheme } = useTheme();
  return (
    <Magnetic>
      <button onClick={() => setTheme(theme === "light" ? "dark" : "light")}
        className="p-3 rounded-full border border-current/20 hover:bg-current/5 transition-colors"
        aria-label="다크/라이트 모드 토글">
        {theme === "light" ? <Moon size={20} /> : <Sun size={20} />}
      </button>
    </Magnetic>
  );
}
```

- [ ] **Step 1: 작성 + 커밋**

```bash
git add components/layout/theme-toggle.tsx
git commit -m "feat(layout): theme toggle (Sun/Moon + Magnetic)"
```

### Task 4.3: Nav (top fixed) + Footer

**Files:** Create `components/layout/nav.tsx`, `components/layout/footer.tsx`

```tsx
// components/layout/nav.tsx
"use client";
import Link from "next/link";
import { ThemeToggle } from "./theme-toggle";

const LINKS = [
  { href: "/about", label: "About" },
  { href: "/career", label: "Career" },
  { href: "/marketing", label: "Marketing" },
  { href: "/builder", label: "AI Builder" },
  { href: "/contact", label: "Contact" },
];

export function Nav() {
  return (
    <nav className="fixed top-0 left-0 right-0 z-50 px-6 md:px-10 lg:px-16 py-4 flex items-center justify-between mix-blend-difference text-white">
      <Link href="/" className="text-display-md font-display">정인수</Link>
      <ul className="hidden md:flex gap-8 text-meta">
        {LINKS.map(l => <li key={l.href}><Link href={l.href} className="hover:opacity-70 transition-opacity">{l.label}</Link></li>)}
      </ul>
      <div className="mix-blend-normal text-current"><ThemeToggle /></div>
    </nav>
  );
}
```

```tsx
// components/layout/footer.tsx
import Link from "next/link";

export function Footer() {
  return (
    <footer className="px-6 md:px-10 lg:px-16 py-16 border-t border-current/15">
      <div className="flex flex-col md:flex-row justify-between gap-8">
        <div>
          <p className="text-display-md font-display mb-4">정인수</p>
          <p className="text-meta">AI Builder · 17년 마케터 · KOICA·정부 부처 강사</p>
        </div>
        <div className="text-meta space-y-2">
          <p><a href="mailto:9843ohs@gmail.com" className="hover:text-orange-500 transition-colors">9843ohs@gmail.com</a></p>
          <p><Link href="https://github.com/junginsu-make" className="hover:text-orange-500 transition-colors">github.com/junginsu-make</Link></p>
        </div>
      </div>
    </footer>
  );
}
```

- [ ] **Step 1: 작성 + 커밋**

```bash
git add components/layout/nav.tsx components/layout/footer.tsx
git commit -m "feat(layout): Nav (mix-blend-difference top fixed) + Footer"
```

### Task 4.4: Root layout 통합 (Theme + Lenis + Nav + Footer)

**Files:** Modify `app/layout.tsx`

- [ ] **Step 1: 통합**

```tsx
import type { Metadata } from "next";
import { Fraunces, JetBrains_Mono } from "next/font/google";
import "./globals.css";
import { ThemeProvider } from "@/lib/theme";
import { LenisProvider } from "@/components/motion/lenis-provider";
import { Nav } from "@/components/layout/nav";
import { Footer } from "@/components/layout/footer";

const fraunces = Fraunces({ subsets: ["latin"], variable: "--font-fraunces", axes: ["SOFT", "WONK", "opsz"], display: "swap" });
const jetbrains = JetBrains_Mono({ subsets: ["latin"], variable: "--font-jetbrains", display: "swap" });

export const metadata: Metadata = {
  title: "정인수 — AI 시대를 만난 17년 마케터, 풀사이클 빌더",
  description: "AI Builder · 마케팅 17년 · GitHub 84 · make.com 4 핵심 · 6 라이브 SaaS · KOICA·정부 부처 AI 강의",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="ko" className={`${fraunces.variable} ${jetbrains.variable}`} suppressHydrationWarning>
      <body>
        <ThemeProvider>
          <LenisProvider>
            <Nav />
            <main>{children}</main>
            <Footer />
          </LenisProvider>
        </ThemeProvider>
      </body>
    </html>
  );
}
```

- [ ] **Step 2: 검증**

```bash
pnpm dev
# 브라우저: http://localhost:3000
# - 우측 상단 토글 작동 (Light ↔ Dark)
# - 스크롤 부드러움 (Lenis)
# - Nav가 mix-blend-difference로 배경에 따라 반전
```

- [ ] **Step 3: 커밋**

```bash
git add app/layout.tsx
git commit -m "feat(layout): root layout integration (Theme + Lenis + Nav + Footer)"
```

---

## Phase 5 — shadcn/ui Primitives (필요한 것만)

### Task 5.1: Button (variant + Magnetic wrapper)

**Files:** Create `components/ui/button.tsx`, `lib/utils.ts`

- [ ] **Step 1: utils**

```ts
// lib/utils.ts
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";
export function cn(...inputs: ClassValue[]) { return twMerge(clsx(inputs)); }
```

- [ ] **Step 2: Button (shadcn/ui 표준)**

```tsx
// components/ui/button.tsx
import * as React from "react";
import { Slot } from "@radix-ui/react-slot";
import { cva, type VariantProps } from "class-variance-authority";
import { cn } from "@/lib/utils";

const buttonVariants = cva(
  "inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-full transition-all focus-visible:outline-none focus-visible:ring-2 disabled:pointer-events-none disabled:opacity-50",
  {
    variants: {
      variant: {
        default: "bg-current text-[var(--bg)] hover:opacity-90",
        outline: "border border-current/30 hover:bg-current/5",
        ghost: "hover:bg-current/5",
        accent: "bg-[var(--accent)] text-[var(--bg)] hover:opacity-90",
      },
      size: {
        default: "h-12 px-6 text-body",
        sm: "h-9 px-4 text-meta",
        lg: "h-16 px-10 text-body-lg",
      },
    },
    defaultVariants: { variant: "default", size: "default" },
  },
);

export interface ButtonProps extends React.ButtonHTMLAttributes<HTMLButtonElement>, VariantProps<typeof buttonVariants> { asChild?: boolean; }
export const Button = React.forwardRef<HTMLButtonElement, ButtonProps>(({ className, variant, size, asChild = false, ...props }, ref) => {
  const Comp = asChild ? Slot : "button";
  return <Comp className={cn(buttonVariants({ variant, size, className }))} ref={ref} {...props} />;
});
Button.displayName = "Button";
```

- [ ] **Step 3: 커밋**

```bash
git add lib/utils.ts components/ui/button.tsx
git commit -m "feat(ui): Button (shadcn/ui pattern + accent variant)"
```

### Task 5.2~5.6: Card / Dialog / Tabs / Tooltip / Switch

> 표준 shadcn/ui 패턴. Radix wrapper. 자세한 코드는 https://ui.shadcn.com 표준 따름.

- [ ] **Step 1: 5개 파일 생성** (Card, Dialog, Tabs, Tooltip, Switch — shadcn 표준)

```bash
# 각 파일을 shadcn/ui 표준 구현으로 작성
# components/ui/card.tsx, dialog.tsx, tabs.tsx, tooltip.tsx, switch.tsx
```

각 파일 내용은 shadcn/ui 공식 등록 (Radix wrapper). 페이퍼 톤에 맞춰 색상만 var(--fg)/var(--bg) 사용.

- [ ] **Step 2: 한 번에 커밋**

```bash
git add components/ui
git commit -m "feat(ui): Card/Dialog/Tabs/Tooltip/Switch (shadcn 표준 + 페이퍼 톤)"
```

---

## Phase 6 — Home (`/`) 600vh

### Task 6.1: 홈 root + 6 핀 섹션 컨테이너

**Files:** Modify `app/page.tsx`, Create `components/home/{manifesto,counter-section,saas-cycle,three-categories,duality,cta}.tsx`

- [ ] **Step 1: page.tsx 골격**

```tsx
// app/page.tsx
import { Manifesto } from "@/components/home/manifesto";
import { CounterSection } from "@/components/home/counter-section";
import { SaasCycle } from "@/components/home/saas-cycle";
import { ThreeCategories } from "@/components/home/three-categories";
import { Duality } from "@/components/home/duality";
import { Cta } from "@/components/home/cta";

export default function Home() {
  return (
    <>
      <Manifesto />
      <CounterSection />
      <SaasCycle />
      <ThreeCategories />
      <Duality />
      <Cta />
    </>
  );
}
```

- [ ] **Step 2: 6 컴포넌트 placeholder**

각 컴포넌트는 우선 빈 100vh 섹션:

```tsx
// components/home/manifesto.tsx
import { PinSection } from "@/components/motion/pin-section";
export function Manifesto() {
  return <PinSection><div className="h-screen flex items-center justify-center text-display-md">Manifesto</div></PinSection>;
}
```

(나머지 5개 동일 패턴)

- [ ] **Step 3: 검증 + 커밋**

```bash
pnpm dev
# 브라우저: 6개 섹션 핀 스크롤 작동, 각 섹션 100vh
git add app/page.tsx components/home
git commit -m "feat(home): 6 핀 섹션 골격"
```

### Task 6.2: 매니페스토 키네틱 (Section 0 — 0vh)

**Files:** Modify `components/home/manifesto.tsx`

- [ ] **Step 1: 구현**

```tsx
"use client";
import { PinSection } from "@/components/motion/pin-section";
import { KineticText } from "@/components/motion/kinetic-text";
import { MANIFESTO, HERO_META } from "@/lib/data/home";

export function Manifesto() {
  return (
    <PinSection className="relative overflow-hidden">
      <div className="h-screen flex items-end px-6 md:px-10 lg:px-16 pb-20">
        <h1 className="text-display-mega font-display max-w-[20ch] leading-[0.9]">
          <KineticText>{MANIFESTO}</KineticText>
        </h1>
        <div className="absolute bottom-10 right-6 md:right-10 lg:right-16 text-meta">
          {HERO_META.name} / {HERO_META.birth} / {HERO_META.city} / {HERO_META.role}
        </div>
      </div>
    </PinSection>
  );
}
```

- [ ] **Step 2: 검증 + 커밋**

```bash
pnpm dev
# 매니페스토 텍스트가 마우스 따라 SOFT/WONK axis 변형
git add components/home/manifesto.tsx
git commit -m "feat(home): manifesto 키네틱 타입 (Fraunces axes follow mouse)"
```

### Task 6.3: Counter (Section 1 — 100vh)

**Files:** Modify `components/home/counter-section.tsx`

- [ ] **Step 1: 구현**

```tsx
"use client";
import { Counter } from "@/components/motion/counter";
import { COUNTERS, TECH_BADGES } from "@/lib/data/home";

export function CounterSection() {
  return (
    <section className="min-h-screen flex flex-col justify-center px-6 md:px-10 lg:px-16 py-24">
      <div className="grid grid-cols-2 md:grid-cols-4 gap-y-16">
        {COUNTERS.map((c, i) => (
          <div key={i}>
            <p className="text-display-xl font-display"><Counter to={parseInt(c.value)} suffix={c.suffix} /></p>
            <p className="text-meta mt-4">{c.label}</p>
          </div>
        ))}
      </div>
      <div className="mt-24 text-meta opacity-60 max-w-3xl flex flex-wrap gap-3">
        {TECH_BADGES.map(b => <span key={b}>· {b}</span>)}
      </div>
    </section>
  );
}
```

- [ ] **Step 2: 검증 + 커밋**

```bash
git add components/home/counter-section.tsx
git commit -m "feat(home): counter section (17/84/4/6 + tech badges)"
```

### Task 6.4: 6 SaaS Cycle (Section 2 — 200vh)

**Files:** Modify `components/home/saas-cycle.tsx`

- [ ] **Step 1: 구현**

```tsx
"use client";
import { useState, useEffect } from "react";
import { SAAS_LIST } from "@/lib/data/saas";
import { SweepLink } from "@/components/motion/color-sweep";
import { PinSection } from "@/components/motion/pin-section";

export function SaasCycle() {
  const [idx, setIdx] = useState(0);
  const [paused, setPaused] = useState(false);

  useEffect(() => {
    if (paused) return;
    const t = setInterval(() => setIdx(i => (i + 1) % SAAS_LIST.length), 3000);
    return () => clearInterval(t);
  }, [paused]);

  const saas = SAAS_LIST[idx];
  return (
    <PinSection>
      <div onMouseEnter={() => setPaused(true)} onMouseLeave={() => setPaused(false)}
        className="relative h-screen overflow-hidden">
        {SAAS_LIST.map((s, i) => (
          <SweepLink key={s.slug} href={`/builder/${s.slug}`}
            className={`absolute inset-0 transition-opacity duration-1000 ${i === idx ? "opacity-100" : "opacity-0"}`}>
            <picture>
              <source srcSet={`/captured/${s.capturedSlug}/home/desktop.avif`} type="image/avif" />
              <source srcSet={`/captured/${s.capturedSlug}/home/desktop.webp`} type="image/webp" />
              <img src={`/captured/${s.capturedSlug}/home/desktop.jpg`} alt={s.name} className="w-full h-full object-cover" />
            </picture>
          </SweepLink>
        ))}
        <div className="absolute bottom-10 left-6 md:left-10 lg:left-16 text-display-md font-display text-white mix-blend-difference">
          {saas.name} <span className="text-body opacity-80 ml-4">{saas.tagline.split(" — ")[0]}</span>
        </div>
        <div className="absolute bottom-10 right-6 md:right-10 lg:right-16 text-meta text-white mix-blend-difference">
          {idx + 1} / {SAAS_LIST.length}
        </div>
      </div>
    </PinSection>
  );
}
```

- [ ] **Step 2: 검증 + 커밋**

```bash
git add components/home/saas-cycle.tsx
git commit -m "feat(home): 6 SaaS auto fade cycle 3s + 컬러 스윕 클릭"
```

### Task 6.5: 3 분류 인덱스 (Section 3 — 300vh)

**Files:** Modify `components/home/three-categories.tsx`

```tsx
import { SweepLink } from "@/components/motion/color-sweep";

const CATEGORIES = [
  { href: "/career", label: "경력 · 자격", img: "/photos/profile-hero.png", caption: "17년 마케팅 + AI 강의 + 풀사이클 빌더" },
  { href: "/marketing", label: "마케팅", img: "/photos/teaching/IMG_1187.jpg", caption: "KOICA · 광고 운영 10년 · 30+ 강의" },
  { href: "/builder", label: "AI Builder", img: "/captured/tickpoint/home/desktop.jpg", caption: "GitHub 84 · make.com 4 핵심 · 6 라이브 SaaS" },
];

export function ThreeCategories() {
  return (
    <section className="min-h-screen px-6 md:px-10 lg:px-16 py-32">
      <h2 className="text-display-lg font-display mb-16">3 분류로 보는 정인수</h2>
      <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
        {CATEGORIES.map(c => (
          <SweepLink key={c.href} href={c.href} className="group block aspect-[3/4] relative overflow-hidden border border-current/15">
            <img src={c.img} alt={c.label} className="absolute inset-0 w-full h-full object-cover opacity-0 group-hover:opacity-90 transition-opacity duration-500" />
            <div className="relative h-full flex flex-col justify-end p-8 group-hover:text-white mix-blend-difference">
              <h3 className="text-display-md font-display">{c.label}</h3>
              <p className="text-meta mt-3">{c.caption}</p>
            </div>
          </SweepLink>
        ))}
      </div>
    </section>
  );
}
```

- [ ] **Step 1: 작성 + 커밋**

```bash
git add components/home/three-categories.tsx
git commit -m "feat(home): 3 분류 거대 카드 (호버 시 사진 fill + mix-blend)"
```

### Task 6.6: 양면성 (Section 4 — 400vh)

**Files:** Modify `components/home/duality.tsx`

```tsx
import { PinSection } from "@/components/motion/pin-section";

export function Duality() {
  return (
    <PinSection>
      <div className="h-screen grid grid-cols-1 md:grid-cols-2 relative">
        <div className="relative bg-paper">
          <img src="/photos/teaching/IMG_1187.jpg" alt="강사" className="absolute inset-0 w-full h-full object-cover opacity-40" />
          <div className="relative h-full flex flex-col justify-end p-8 md:p-16">
            <h3 className="text-display-md font-display">강사</h3>
            <p className="text-meta mt-3">sbcyberpass@naver.com</p>
            <p className="text-body-lg mt-4 max-w-md">KOICA · 정부 부처 · 대학 · 소상공인 30+ 강의</p>
          </div>
        </div>
        <div className="relative bg-ink-dark text-paper-dark">
          <img src="/captured/tickpoint/home/desktop.jpg" alt="빌더" className="absolute inset-0 w-full h-full object-cover opacity-50" />
          <div className="relative h-full flex flex-col justify-end p-8 md:p-16">
            <h3 className="text-display-md font-display">AI 빌더</h3>
            <p className="text-meta mt-3">9843ohs@gmail.com</p>
            <p className="text-body-lg mt-4 max-w-md">GitHub 84 · 6 라이브 SaaS · make.com 4 핵심</p>
          </div>
        </div>
        <div className="absolute inset-0 flex items-center justify-center pointer-events-none">
          <div className="bg-orange/95 text-white p-8 md:p-12 rounded-full">
            <p className="text-display-md font-display">한 사람, 두 면</p>
          </div>
        </div>
      </div>
    </PinSection>
  );
}
```

- [ ] **Step 1: 작성 + 커밋**

```bash
git add components/home/duality.tsx
git commit -m "feat(home): 양면성 split layout (강사 / AI 빌더 + 중앙 카피)"
```

### Task 6.7: CTA (Section 5 — 500vh)

**Files:** Modify `components/home/cta.tsx`

```tsx
import { Magnetic } from "@/components/motion/magnetic";
import { CONTACT_EMAIL, CTA_QUOTE } from "@/lib/data/home";

export function Cta() {
  return (
    <section className="min-h-screen bg-ink-dark text-paper-dark flex flex-col items-center justify-center px-6 md:px-10 lg:px-16 py-32 text-center">
      <p className="text-meta mb-8">{CTA_QUOTE}</p>
      <Magnetic strength={0.6}>
        <a href={`mailto:${CONTACT_EMAIL}`} className="text-display-mega font-display hover:text-orange-dark transition-colors">
          {CONTACT_EMAIL}
        </a>
      </Magnetic>
      <a href="/resume.pdf" download className="text-meta mt-12 underline-offset-4 hover:underline">이력서 PDF 다운로드 →</a>
    </section>
  );
}
```

- [ ] **Step 1: 작성 + 커밋**

```bash
git add components/home/cta.tsx
git commit -m "feat(home): CTA (마그네틱 메일 + 이력서 PDF)"
```

### Task 6.8: 홈 페이지 풀 검증

- [ ] **Step 1: dev 서버 + 브라우저 풀 시나리오**

```bash
pnpm dev
```

브라우저 검증:
- 0vh 매니페스토 → 마우스 따라 글자 변형
- 100vh 카운터 → 4 숫자 카운트업
- 200vh SaaS 사이클 → 3초마다 페이드 + 호버 정지 + 클릭 시 컬러 스윕
- 300vh 3 분류 → 호버 시 사진 fill
- 400vh 양면성 → split + 중앙 오렌지 원형
- 500vh CTA → 마그네틱 메일 호버 + mailto: 작동
- 다크/라이트 토글 시 모든 섹션 색상 자연스럽게 전환

- [ ] **Step 2: 모바일 시뮬레이션 (375px) 검증**

dev tools → Toggle device toolbar → iPhone SE → 모든 섹션 깨짐 없는지

- [ ] **Step 3: 콘솔 에러 0 검증**

dev tools Console 탭 → 에러 없어야 함

- [ ] **Step 4: 검증 commit (verification-only)**

```bash
git commit --allow-empty -m "verify(home): 6 핀 섹션 풀 시나리오 + 모바일 + 콘솔 0 에러"
```

---

## Phase 7 — `/about`

### Task 7.1: 5 챕터 + 풀블리드 히어로 + 양면성 벤 다이어그램

**Files:** Create `app/about/page.tsx`, `components/about/{chapters.tsx,duality-venn.tsx}`

- [ ] **Step 1: page.tsx + 컴포넌트 구현**

```tsx
// app/about/page.tsx
import { Chapters } from "@/components/about/chapters";
import { DualityVenn } from "@/components/about/duality-venn";

export default function About() {
  return (
    <>
      <section className="relative h-screen">
        <img src="/photos/profile-hero.png" alt="정인수" className="absolute inset-0 w-full h-full object-cover" />
        <div className="absolute bottom-10 left-6 md:left-10 lg:left-16 text-paper-dark mix-blend-difference">
          <h1 className="text-display-xl font-display">정인수</h1>
          <p className="text-meta mt-3">1983 · 서울 · Palette ㈜ Agent 본부 팀장</p>
        </div>
      </section>
      <Chapters />
      <DualityVenn />
    </>
  );
}
```

```tsx
// components/about/chapters.tsx
const CHAPTERS = [
  { title: "성장기", body: "군 장교 + 성직자 부친 가정에서 위계·희생·책임감 학습. 늦깎이 사회복지학과 입학 (영동대 4년제, 학점 3.98/4.5)", image: "..." },
  { title: "대학·총학생회 (2006~2010)", body: "총 학회장 · 16대 총학생회 복지국장 · 학과조교 회장. 졸업 후 학과조교 — 우수교직원 수상", image: "..." },
  { title: "영업·마케팅 17년 (2001~)", body: "캐드뱅크 최연소 주임 → 영동대 학과조교 → 학원·교육원·평생교육 → 티몬 광고대행 (연 40~60억)", image: "..." },
  { title: "AI/강의 진입 (2018~)", body: "솔찍한인쌤 시작. KOICA·대학·소상공인·정부 부처 강의", image: "..." },
  { title: "풀사이클 AI 빌더 (2024.10~)", body: "11개월 81 SYSTEM 자동화 + 6 라이브 SaaS + GitHub 84 + Palette Agent 본부 팀장", image: "/captured/tickpoint/home/desktop.jpg" },
];
export function Chapters() {
  return (
    <section className="px-6 md:px-10 lg:px-16 py-32 space-y-32">
      {CHAPTERS.map((c, i) => (
        <div key={i} className="grid md:grid-cols-2 gap-16 items-center">
          <div>
            <p className="text-meta opacity-60">CHAPTER {i + 1}</p>
            <h3 className="text-display-md font-display mt-4">{c.title}</h3>
            <p className="text-body-lg mt-6 max-w-prose">{c.body}</p>
          </div>
          <div className="aspect-[4/3] bg-current/5"></div>
        </div>
      ))}
    </section>
  );
}
```

```tsx
// components/about/duality-venn.tsx (커스텀 SVG)
export function DualityVenn() {
  return (
    <section className="px-6 md:px-10 lg:px-16 py-32 text-center">
      <h2 className="text-display-md font-display mb-16">한 사람, 두 면</h2>
      <svg viewBox="0 0 800 500" className="mx-auto max-w-3xl">
        <circle cx="320" cy="250" r="200" fill="none" stroke="currentColor" strokeOpacity="0.4" />
        <circle cx="480" cy="250" r="200" fill="none" stroke="var(--color-orange)" strokeWidth="2" />
        <text x="220" y="120" className="text-meta">강사·블로거</text>
        <text x="220" y="260" textAnchor="middle" className="text-meta">KOICA · 대학 · 소상공인</text>
        <text x="580" y="120" className="text-meta" textAnchor="end">AI 빌더</text>
        <text x="580" y="260" textAnchor="middle" className="text-meta">GitHub 84 · 6 SaaS · make.com 4</text>
        <text x="400" y="240" textAnchor="middle" className="text-display-md font-display">콘텐츠 자동화</text>
        <text x="400" y="270" textAnchor="middle" className="text-meta">강의 + 운영</text>
      </svg>
    </section>
  );
}
```

- [ ] **Step 2: 검증 + 커밋**

```bash
pnpm dev
# /about 풀 검증
git add app/about components/about
git commit -m "feat(about): 풀블리드 히어로 + 5 챕터 + 양면성 벤 다이어그램"
```

---

## Phase 8 — `/career` (6 임팩트 스트림 + 자격증)

### Task 8.1: ImpactStreams 컴포넌트

**Files:** Create `app/career/page.tsx`, `components/career/{impact-streams,company-timeline-toggle,certifications}.tsx`

- [ ] **Step 1: ImpactStreams**

```tsx
// components/career/impact-streams.tsx
import { IMPACT_STREAMS } from "@/lib/data/career";

export function ImpactStreams() {
  return (
    <section className="px-6 md:px-10 lg:px-16 py-32">
      <h1 className="text-display-lg font-display mb-4">17년 4개월</h1>
      <p className="text-display-md font-display opacity-50 mb-24">6 임팩트 스트림</p>
      <div className="space-y-32">
        {IMPACT_STREAMS.map((s, i) => (
          <article key={s.id} className="grid md:grid-cols-12 gap-8 border-t border-current/15 pt-12">
            <div className="md:col-span-2">
              <p className="text-display-md font-display opacity-30">{s.number}</p>
            </div>
            <div className="md:col-span-10">
              <h2 className="text-display-md font-display mb-8">{s.title}</h2>
              <ul className="space-y-4">
                {s.bullets.map((b, j) => <li key={j} className="text-body-lg max-w-prose"><span className="opacity-50 text-meta mr-3">{(j + 1).toString().padStart(2, "0")}</span>{b}</li>)}
              </ul>
              <p className="text-meta opacity-50 mt-8">{s.companies.join(" / ")}</p>
            </div>
          </article>
        ))}
      </div>
    </section>
  );
}
```

- [ ] **Step 2: 17 회사 풀 토글**

```tsx
// components/career/company-timeline-toggle.tsx
"use client";
import { useState } from "react";
import { CAREER_FULL } from "@/lib/data/career";

export function CompanyTimelineToggle() {
  const [open, setOpen] = useState(false);
  return (
    <section className="px-6 md:px-10 lg:px-16 py-16 border-t border-current/15">
      <button onClick={() => setOpen(!open)} className="text-meta opacity-60 hover:opacity-100">
        {open ? "▲" : "▼"} 17 회사 풀 타임라인 {open ? "닫기" : "보기"}
      </button>
      {open && (
        <div className="mt-12 space-y-4">
          {CAREER_FULL.map((c, i) => (
            <div key={i} className="grid grid-cols-12 gap-4 py-3 border-b border-current/10 text-body">
              <span className="col-span-3 text-meta opacity-60">{c.period}</span>
              <span className="col-span-3 font-medium">{c.company}{c.current && <sup className="text-orange ml-1">●</sup>}</span>
              <span className="col-span-2 text-meta">{c.role}</span>
              <span className="col-span-4 opacity-80">{c.impact}</span>
            </div>
          ))}
        </div>
      )}
    </section>
  );
}
```

- [ ] **Step 3: Certifications**

```tsx
// components/career/certifications.tsx
import { CERTS_VISUAL, CERTS_TEXT } from "@/lib/data/certifications";

export function Certifications() {
  return (
    <section className="px-6 md:px-10 lg:px-16 py-32 border-t border-current/15">
      <h2 className="text-display-md font-display mb-16">자격증 9</h2>
      <div className="grid md:grid-cols-3 gap-8 mb-24">
        {CERTS_VISUAL.map(c => (
          <div key={c.name} className="border border-current/15 p-6">
            <div className="aspect-[3/4] bg-current/5 mb-6 overflow-hidden">
              <img src={c.image} alt={c.name} className="w-full h-full object-cover" />
            </div>
            <p className="text-meta opacity-60">{c.year}</p>
            <h3 className="text-body-lg font-medium mt-2">{c.name}</h3>
            <p className="text-meta opacity-60 mt-2">{c.issuer}</p>
          </div>
        ))}
      </div>
      <div className="space-y-3 text-body">
        {CERTS_TEXT.map(c => (
          <div key={c.name} className="grid grid-cols-12 gap-4 py-2 border-b border-current/10">
            <span className="col-span-7">{c.name}</span>
            <span className="col-span-2 text-meta opacity-60">{c.year}</span>
            <span className="col-span-3 text-meta opacity-60">{c.issuer}</span>
          </div>
        ))}
      </div>
    </section>
  );
}
```

- [ ] **Step 4: page.tsx**

```tsx
// app/career/page.tsx
import { ImpactStreams } from "@/components/career/impact-streams";
import { CompanyTimelineToggle } from "@/components/career/company-timeline-toggle";
import { Certifications } from "@/components/career/certifications";

export default function Career() {
  return (
    <>
      <ImpactStreams />
      <CompanyTimelineToggle />
      <Certifications />
    </>
  );
}
```

- [ ] **Step 5: 검증 + 커밋**

```bash
git add app/career components/career
git commit -m "feat(career): 6 임팩트 스트림 + 17 회사 토글 + 자격증 3 비주얼+6 텍스트"
```

---

## Phase 9 — `/marketing` (차트 + 다이어그램)

### Task 9.1: KPI 차트 + AI PL 클라이언트 카드

**Files:** Create `app/marketing/page.tsx`, `components/marketing/kpi-charts.tsx`, `components/marketing/ai-clients.tsx`

- [ ] **Step 1: KPI 차트 (Recharts 막대)**

```tsx
// components/marketing/kpi-charts.tsx
"use client";
import { BarChart, Bar, XAxis, YAxis, ResponsiveContainer, Tooltip } from "recharts";
import { COMPANY_AD_BUDGET } from "@/lib/data/marketing";

export function KpiCharts() {
  const data = COMPANY_AD_BUDGET.map(c => ({ name: c.company, value: parseInt(c.budget.match(/\d+/)?.[0] ?? "0"), note: c.note }));
  return (
    <section className="px-6 md:px-10 lg:px-16 py-32">
      <h2 className="text-display-md font-display mb-12">광고 운영 KPI</h2>
      <div className="h-96">
        <ResponsiveContainer>
          <BarChart data={data}>
            <XAxis dataKey="name" tick={{ fontSize: 13 }} />
            <YAxis tick={{ fontSize: 13 }} unit="억" />
            <Tooltip />
            <Bar dataKey="value" fill="var(--color-orange)" />
          </BarChart>
        </ResponsiveContainer>
      </div>
    </section>
  );
}
```

- [ ] **Step 2: AI PL 클라이언트 카드 + AI Content Op + Capsule PM**

```tsx
// components/marketing/ai-clients.tsx
import { AI_PL_CLIENTS, AI_CONTENT_OP_CLIENTS, CAPSULE_PM_PROJECTS, CAPSULE_PM_OTHERS } from "@/lib/data/marketing";

export function AiClients() {
  return (
    <section className="px-6 md:px-10 lg:px-16 py-32 space-y-24 border-t border-current/15">
      <div>
        <h2 className="text-display-md font-display mb-8">AI SaaS PL · 진행 중 4건</h2>
        <div className="grid grid-cols-2 md:grid-cols-4 gap-6">
          {AI_PL_CLIENTS.map(c => (
            <div key={c} className="border border-current/15 p-6 text-center">
              <p className="text-body-lg font-medium">{c}</p>
              <p className="text-meta opacity-60 mt-2">진행 중 ●</p>
            </div>
          ))}
        </div>
      </div>
      <div>
        <h2 className="text-display-md font-display mb-8">AI Content Operation · 7+ 클라이언트</h2>
        <div className="flex flex-wrap gap-3 text-meta">
          {AI_CONTENT_OP_CLIENTS.map(c => <span key={c} className="border border-current/30 px-4 py-2">{c}</span>)}
        </div>
      </div>
      <div>
        <h2 className="text-display-md font-display mb-8">Capsule Media · 종합홍보 PM 20+</h2>
        <div className="space-y-3">
          {CAPSULE_PM_PROJECTS.map(p => <div key={p.name} className="flex justify-between border-b border-current/10 py-3 text-body"><span>{p.name}</span><span className="text-meta opacity-60">{p.role}</span></div>)}
          <p className="text-meta opacity-60 mt-3">{CAPSULE_PM_OTHERS}</p>
        </div>
      </div>
    </section>
  );
}
```

- [ ] **Step 3: page.tsx + 커밋**

```tsx
// app/marketing/page.tsx
import { KpiCharts } from "@/components/marketing/kpi-charts";
import { AiClients } from "@/components/marketing/ai-clients";

export default function Marketing() {
  return (
    <>
      <section className="px-6 md:px-10 lg:px-16 pt-32 pb-12">
        <h1 className="text-display-lg font-display">마케팅 임팩트</h1>
      </section>
      <KpiCharts />
      <AiClients />
    </>
  );
}
```

```bash
git add app/marketing/page.tsx components/marketing/kpi-charts.tsx components/marketing/ai-clients.tsx
git commit -m "feat(marketing): KPI 차트 + AI PL 4 + Content Op 7+ + Capsule PM 20+"
```

---

## Phase 10 — `/marketing/teaching` (3 Tier + 자동 롤링 캐러셀)

### Task 10.1: 자동 롤링 캐러셀

**Files:** Create `components/marketing/teaching-carousel.tsx`

- [ ] **Step 1: 56장 자동 캐러셀 (Ken Burns + parallax)**

```tsx
"use client";
import { useState, useEffect } from "react";

const TEACHING_PHOTOS = [
  "/photos/teaching/KakaoTalk_20200102_190424548_01.jpg",
  "/photos/teaching/IMG_1187.jpg",
  "/photos/teaching/DSC01363.JPG",
  "/photos/teaching/P1070868.JPG",
  // ... 56장 (manifest 사용 또는 자동 디렉토리 스캔)
];

export function TeachingCarousel() {
  const [idx, setIdx] = useState(0);
  const [paused, setPaused] = useState(false);

  useEffect(() => {
    if (paused) return;
    const t = setInterval(() => setIdx(i => (i + 1) % TEACHING_PHOTOS.length), 3000);
    return () => clearInterval(t);
  }, [paused]);

  return (
    <section className="relative h-screen overflow-hidden" onMouseEnter={() => setPaused(true)} onMouseLeave={() => setPaused(false)}>
      {TEACHING_PHOTOS.map((p, i) => (
        <div key={p} className={`absolute inset-0 transition-opacity duration-1000 ${i === idx ? "opacity-100" : "opacity-0"}`}>
          <img src={p} alt="강의" className={`w-full h-full object-cover ${i === idx ? "animate-ken-burns" : ""}`} />
        </div>
      ))}
      <div className="absolute bottom-10 right-6 md:right-10 lg:right-16 text-meta text-white mix-blend-difference">
        {idx + 1} / {TEACHING_PHOTOS.length}
      </div>
    </section>
  );
}
```

`globals.css`에 추가:
```css
@keyframes ken-burns { 0% { transform: scale(1) translate(0,0); } 100% { transform: scale(1.08) translate(-2%, -1%); } }
.animate-ken-burns { animation: ken-burns 4s ease-out forwards; }
```

### Task 10.2: 3 Tier 카드

**Files:** Create `components/marketing/teaching-tiers.tsx`

```tsx
import { TIER1, TIER2, TIER3 } from "@/lib/data/teaching";

export function TeachingTiers() {
  return (
    <>
      <section className="px-6 md:px-10 lg:px-16 py-32">
        <p className="text-meta opacity-60">TIER 01 · 정부·공공·대학·AI 교육</p>
        <h2 className="text-display-md font-display mt-4 mb-16">메인 강의처</h2>
        <div className="grid md:grid-cols-3 gap-8">
          {TIER1.map(t => (
            <div key={t.name} className="border border-current/15">
              <div className="aspect-[4/3] overflow-hidden bg-current/5">
                <img src={t.image} alt={t.name} className="w-full h-full object-cover" />
              </div>
              <div className="p-6">
                <p className="text-meta opacity-60">{t.category}</p>
                <h3 className="text-body-lg font-medium mt-2">{t.name}</h3>
                {t.desc && <p className="text-meta opacity-60 mt-2">{t.desc}</p>}
              </div>
            </div>
          ))}
        </div>
      </section>
      <section className="px-6 md:px-10 lg:px-16 py-32 border-t border-current/15">
        <p className="text-meta opacity-60">TIER 02 · 협회·기업·연합회</p>
        <h2 className="text-display-md font-display mt-4 mb-16">단체 강의</h2>
        <div className="grid grid-cols-2 md:grid-cols-4 gap-6">
          {TIER2.map(t => <div key={t.name} className="border border-current/15 p-6"><p className="text-body">{t.name}</p></div>)}
        </div>
      </section>
      <section className="px-6 md:px-10 lg:px-16 py-32 border-t border-current/15">
        <p className="text-meta opacity-60">TIER 03 · 개인·자영업자</p>
        <h2 className="text-display-md font-display mt-4 mb-16">개인 강의 (텍스트)</h2>
        <div className="flex flex-wrap gap-3 text-meta">
          {TIER3.map(t => <span key={t} className="border border-current/30 px-3 py-1">{t}</span>)}
        </div>
      </section>
    </>
  );
}
```

- [ ] **Step 1: page.tsx + 커밋**

```tsx
// app/marketing/teaching/page.tsx
import { TeachingCarousel } from "@/components/marketing/teaching-carousel";
import { TeachingTiers } from "@/components/marketing/teaching-tiers";

export default function Teaching() {
  return (
    <>
      <TeachingCarousel />
      <TeachingTiers />
    </>
  );
}
```

```bash
git add app/marketing/teaching components/marketing/teaching-carousel.tsx components/marketing/teaching-tiers.tsx app/globals.css
git commit -m "feat(teaching): 자동 롤링 캐러셀 + 3 Tier 카드"
```

---

## Phase 11 — `/marketing/content` (15+ url)

> 콘텐츠 url은 `public/content-refs/콘텐츠 레퍼런스.txt`에서 동적 파싱.

### Task 11.1: 콘텐츠 url 카드 그리드

**Files:** Create `app/marketing/content/page.tsx`, `components/marketing/content-grid.tsx`

- [ ] **Step 1: 카드 그리드 (Playwright 캡처 활용)**

```tsx
// app/marketing/content/page.tsx
import { readFile } from "fs/promises";
import path from "path";

async function getContentRefs() {
  // 자료 폴더의 콘텐츠 레퍼런스.txt 파싱 또는 captured-meta.json에서 가져옴
  try {
    const meta = JSON.parse(await readFile("lib/captured-meta.json", "utf-8"));
    return meta.filter((m: any) => m.ok && m.slug.startsWith("content/"));
  } catch { return []; }
}

export default async function Content() {
  const refs = await getContentRefs();
  return (
    <section className="px-6 md:px-10 lg:px-16 py-32">
      <h1 className="text-display-lg font-display mb-16">콘텐츠 결과물</h1>
      <div className="grid md:grid-cols-3 gap-6">
        {refs.map((r: any) => (
          <a key={r.slug} href={r.url} target="_blank" rel="noreferrer" className="border border-current/15 group">
            <div className="aspect-video overflow-hidden bg-current/5">
              <img src={`/captured/${r.slug.replace("/desktop", "")}/desktop.jpg`} alt={r.url} className="w-full h-full object-cover group-hover:scale-105 transition-transform" />
            </div>
            <div className="p-4">
              <p className="text-meta opacity-60 truncate">{r.url}</p>
            </div>
          </a>
        ))}
      </div>
    </section>
  );
}
```

- [ ] **Step 2: 커밋**

```bash
git add app/marketing/content
git commit -m "feat(content): 캡처된 콘텐츠 url 카드 그리드"
```

---

## Phase 12 — `/builder` 인덱스

### Task 12.1: 6 SaaS 5 bullet 카드 + 카운터 + 4 핵심 미리보기

**Files:** Create `app/builder/page.tsx`, `components/builder/{saas-index.tsx,saas-card-5bullet.tsx,scenarios-preview.tsx}`

- [ ] **Step 1: SaaS 인덱스 5 bullet 카드**

```tsx
// components/builder/saas-card-5bullet.tsx
"use client";
import { useState } from "react";
import { SweepLink } from "@/components/motion/color-sweep";
import type { SaaSDetail } from "@/lib/data/saas";

export function SaasCard({ saas, order }: { saas: SaaSDetail; order: number }) {
  const [hover, setHover] = useState(false);
  return (
    <SweepLink href={`/builder/${saas.slug}`}
      className="block border-b border-current/15 group"
      onMouseEnter={() => setHover(true)} onMouseLeave={() => setHover(false)}>
      <div className="grid grid-cols-12 gap-6 py-12 transition-all">
        <div className="col-span-1 text-meta opacity-50">{order.toString().padStart(2, "0")}</div>
        <div className={`col-span-${hover ? '4' : '3'} transition-all`}>
          <h3 className={`font-display ${hover ? 'text-display-lg' : 'text-display-md'} transition-all`}>{saas.name}</h3>
        </div>
        <div className="col-span-5">
          <p className="text-body opacity-80">{saas.tagline}</p>
          {hover && (
            <ul className="mt-6 space-y-2 text-meta opacity-70">
              {saas.capabilities.slice(0, 5).map((c, i) => <li key={i}>· {c.title}</li>)}
            </ul>
          )}
        </div>
        <div className="col-span-3 aspect-video overflow-hidden bg-current/5">
          <img src={`/captured/${saas.capturedSlug}/home/desktop.jpg`} alt={saas.name} className="w-full h-full object-cover group-hover:scale-105 transition-transform" />
        </div>
      </div>
    </SweepLink>
  );
}
```

- [ ] **Step 2: 인덱스 통합**

```tsx
// app/builder/page.tsx
import { SAAS_LIST } from "@/lib/data/saas";
import { CORE_SCENARIOS } from "@/lib/data/scenarios";
import { GITHUB_TOTAL } from "@/lib/data/github";
import { SaasCard } from "@/components/builder/saas-card-5bullet";
import { Counter } from "@/components/motion/counter";

export default function Builder() {
  return (
    <>
      <section className="px-6 md:px-10 lg:px-16 pt-32 pb-12">
        <h1 className="text-display-lg font-display mb-16">AI Builder</h1>
        <div className="grid grid-cols-2 md:grid-cols-4 gap-y-12">
          <div><p className="text-display-xl font-display"><Counter to={GITHUB_TOTAL} /></p><p className="text-meta mt-2">GitHub Repos</p></div>
          <div><p className="text-display-xl font-display"><Counter to={4} /></p><p className="text-meta mt-2">Make.com Core</p></div>
          <div><p className="text-display-xl font-display"><Counter to={6} /></p><p className="text-meta mt-2">Live SaaS</p></div>
          <div><p className="text-display-xl font-display"><Counter to={17} suffix="+" /></p><p className="text-meta mt-2">AI/SaaS</p></div>
        </div>
      </section>
      <section className="px-6 md:px-10 lg:px-16 py-12">
        <h2 className="text-meta opacity-60 mb-4">6 LIVE SAAS</h2>
        <div>{SAAS_LIST.map((s, i) => <SaasCard key={s.slug} saas={s} order={i + 1} />)}</div>
      </section>
      <section className="px-6 md:px-10 lg:px-16 py-32 border-t border-current/15">
        <h2 className="text-display-md font-display mb-12">make.com 4 핵심 시나리오</h2>
        <div className="grid md:grid-cols-2 gap-8">
          {CORE_SCENARIOS.map(s => (
            <div key={s.number} className="border border-current/15 p-8">
              <p className="text-meta opacity-60">SCENARIO {s.number.toString().padStart(2, "0")}</p>
              <h3 className="text-display-md font-display mt-3">{s.title}</h3>
              <p className="text-body opacity-70 mt-4">{s.flow.join(" → ")}</p>
              <p className="text-meta opacity-50 mt-4">{s.metric}</p>
            </div>
          ))}
        </div>
      </section>
    </>
  );
}
```

- [ ] **Step 3: 검증 + 커밋**

```bash
git add app/builder/page.tsx components/builder
git commit -m "feat(builder): 인덱스 (6 SaaS 5 bullet 카드 + 카운터 + 4 핵심 미리보기)"
```

---

## Phase 13 — `/builder/[slug]` × 6 (트렌디·풍부)

### Task 13.1: 동적 라우트 + 풀블리드 라이브 히어로 + 12 섹션

**Files:** Create `app/builder/[slug]/page.tsx`, `components/builder/saas-detail/*.tsx`

- [ ] **Step 1: 라우트 + generateStaticParams**

```tsx
// app/builder/[slug]/page.tsx
import { notFound } from "next/navigation";
import { SAAS_LIST } from "@/lib/data/saas";
import { SaasHero } from "@/components/builder/saas-detail/hero";
import { SaasCapabilities } from "@/components/builder/saas-detail/capabilities";
import { SaasMetrics } from "@/components/builder/saas-detail/metrics";
import { SaasGallery } from "@/components/builder/saas-detail/full-gallery";
import { SaasNav } from "@/components/builder/saas-detail/prev-next";

export function generateStaticParams() { return SAAS_LIST.map(s => ({ slug: s.slug })); }

export default async function SaasDetail({ params }: { params: Promise<{ slug: string }> }) {
  const { slug } = await params;
  const saas = SAAS_LIST.find(s => s.slug === slug);
  if (!saas) return notFound();

  return (
    <>
      <SaasHero saas={saas} />
      <SaasCapabilities saas={saas} />
      <SaasMetrics saas={saas} />
      <SaasGallery saas={saas} />
      <SaasNav currentSlug={slug} />
    </>
  );
}
```

- [ ] **Step 2: 각 컴포넌트 구현**

```tsx
// components/builder/saas-detail/hero.tsx
import { Magnetic } from "@/components/motion/magnetic";

export function SaasHero({ saas }: { saas: any }) {
  return (
    <section className="relative h-screen">
      <picture>
        <source srcSet={`/captured/${saas.capturedSlug}/home/desktop.avif`} type="image/avif" />
        <source srcSet={`/captured/${saas.capturedSlug}/home/desktop.webp`} type="image/webp" />
        <img src={`/captured/${saas.capturedSlug}/home/desktop.jpg`} alt={saas.name} className="absolute inset-0 w-full h-full object-cover" />
      </picture>
      <div className="absolute bottom-10 left-6 md:left-10 lg:left-16 text-paper-dark mix-blend-difference">
        <p className="text-meta">{saas.toneNote} {saas.since && ` · SINCE ${saas.since}`}</p>
        <h1 className="text-display-xl font-display mt-4">{saas.name}</h1>
        <p className="text-body-lg mt-3 max-w-2xl">{saas.tagline}</p>
        <Magnetic strength={0.3}>
          <a href={saas.liveUrl} target="_blank" rel="noreferrer" className="inline-block mt-8 border border-current px-6 py-3 hover:bg-current hover:text-[var(--bg)] transition-colors">
            Visit Live →
          </a>
        </Magnetic>
      </div>
    </section>
  );
}
```

```tsx
// components/builder/saas-detail/capabilities.tsx
export function SaasCapabilities({ saas }: { saas: any }) {
  return (
    <section className="px-6 md:px-10 lg:px-16 py-32 space-y-32">
      <h2 className="text-display-md font-display opacity-60 mb-12">5+ 능력 풀</h2>
      {saas.capabilities.map((c: any, i: number) => (
        <div key={i} className="grid md:grid-cols-12 gap-8 border-t border-current/15 pt-12">
          <div className="md:col-span-2 text-meta opacity-50">{(i + 1).toString().padStart(2, "0")}</div>
          <div className="md:col-span-5">
            <h3 className="text-display-md font-display">{c.title}</h3>
            <p className="text-body opacity-80 mt-4">{c.description}</p>
          </div>
          <div className="md:col-span-5 aspect-video bg-current/5"></div>
        </div>
      ))}
    </section>
  );
}
```

```tsx
// components/builder/saas-detail/metrics.tsx
import { Counter } from "@/components/motion/counter";

export function SaasMetrics({ saas }: { saas: any }) {
  return (
    <section className="px-6 md:px-10 lg:px-16 py-32 border-t border-current/15">
      <h2 className="text-display-md font-display opacity-60 mb-12">메트릭</h2>
      <div className="grid grid-cols-3 gap-8">
        {saas.metrics.map((m: any, i: number) => (
          <div key={i}>
            <p className="text-display-xl font-display">{m.value}</p>
            <p className="text-meta mt-2 opacity-60">{m.label}</p>
          </div>
        ))}
      </div>
      <div className="mt-16 text-meta opacity-60 flex flex-wrap gap-3">
        {saas.techStack.map((t: string) => <span key={t} className="border border-current/30 px-3 py-1">{t}</span>)}
      </div>
      {saas.github && (
        <div className="mt-12">
          <a href={saas.github} target="_blank" rel="noreferrer" className="text-meta hover:opacity-100 opacity-60 underline-offset-4 hover:underline">
            GitHub: {saas.github.replace("https://github.com/", "")} →
          </a>
        </div>
      )}
    </section>
  );
}
```

```tsx
// components/builder/saas-detail/full-gallery.tsx
"use client";
import { useState } from "react";
import { Dialog, DialogContent } from "@/components/ui/dialog";

export function SaasGallery({ saas }: { saas: any }) {
  const [open, setOpen] = useState<number | null>(null);
  const images = Array.from({ length: saas.folderImageCount }, (_, i) => `${saas.folderPath}/${(i + 1).toString().padStart(2, "0")}.jpg`);

  return (
    <section className="py-32 border-t border-current/15">
      <h2 className="text-display-md font-display opacity-60 px-6 md:px-10 lg:px-16 mb-12">갤러리 ({saas.folderImageCount}장)</h2>
      <div className="overflow-x-auto px-6 md:px-10 lg:px-16">
        <div className="flex gap-4">
          {images.map((src, i) => (
            <button key={i} onClick={() => setOpen(i)} className="flex-shrink-0 w-[80vw] md:w-[40vw] aspect-video bg-current/5 overflow-hidden">
              <img src={src} alt={`${saas.name} ${i + 1}`} className="w-full h-full object-cover hover:scale-105 transition-transform" />
            </button>
          ))}
        </div>
      </div>
      <Dialog open={open !== null} onOpenChange={() => setOpen(null)}>
        <DialogContent className="max-w-7xl">
          {open !== null && <img src={images[open]} alt="" className="w-full h-auto" />}
        </DialogContent>
      </Dialog>
    </section>
  );
}
```

```tsx
// components/builder/saas-detail/prev-next.tsx
import Link from "next/link";
import { SAAS_LIST } from "@/lib/data/saas";

export function SaasNav({ currentSlug }: { currentSlug: string }) {
  const idx = SAAS_LIST.findIndex(s => s.slug === currentSlug);
  const prev = SAAS_LIST[(idx - 1 + SAAS_LIST.length) % SAAS_LIST.length];
  const next = SAAS_LIST[(idx + 1) % SAAS_LIST.length];
  return (
    <section className="px-6 md:px-10 lg:px-16 py-16 border-t border-current/15 flex justify-between text-meta">
      <Link href={`/builder/${prev.slug}`} className="hover:opacity-100 opacity-60">← {prev.name}</Link>
      <Link href={`/builder/${next.slug}`} className="hover:opacity-100 opacity-60">{next.name} →</Link>
    </section>
  );
}
```

- [ ] **Step 3: 검증 — 6 SaaS 모두 방문**

```bash
pnpm dev
# /builder/tickpoint, /builder/lumio, /builder/os-agent, /builder/mkt-automation, /builder/propintel, /builder/architect
# 모두 풀블리드 히어로 + 5 능력 + 메트릭 + 갤러리 + prev/next 작동
```

- [ ] **Step 4: 커밋**

```bash
git add app/builder/[slug] components/builder/saas-detail
git commit -m "feat(builder): 6 SaaS 디테일 페이지 (풀블리드 hero + 5+ 능력 + 메트릭 + 갤러리 + prev/next)"
```

---

## Phase 14 — `/builder/scenarios` (4 핵심 + 81 보조)

### Task 14.1: 4 핵심 풀 (플로우 다이어그램) + 11개월 타임라인 + 81 인벤토리 보조

**Files:** Create `app/builder/scenarios/page.tsx`, `components/builder/scenarios/*.tsx`

- [ ] **Step 1: 4 핵심 시나리오 풀**

```tsx
// app/builder/scenarios/page.tsx
import { CORE_SCENARIOS, INVENTORY_GROUPS, TIMELINE_2024_2025 } from "@/lib/data/scenarios";

export default function Scenarios() {
  return (
    <>
      <section className="px-6 md:px-10 lg:px-16 pt-32 pb-12">
        <p className="text-meta opacity-60">MAIN</p>
        <h1 className="text-display-lg font-display mt-4">make.com 4 핵심 시나리오</h1>
      </section>
      {CORE_SCENARIOS.map(s => (
        <section key={s.number} className="px-6 md:px-10 lg:px-16 py-32 border-t border-current/15">
          <p className="text-meta opacity-50">SCENARIO {s.number.toString().padStart(2, "0")}</p>
          <h2 className="text-display-md font-display mt-4 mb-8">{s.title}</h2>
          <div className="grid md:grid-cols-2 gap-12">
            <div>
              <p className="text-meta opacity-60 mb-4">FLOW</p>
              <div className="flex flex-wrap gap-2 text-meta">
                {s.flow.map((f, i) => (
                  <>
                    <span key={i} className="border border-current/30 px-3 py-1">{f}</span>
                    {i < s.flow.length - 1 && <span className="text-orange opacity-60">→</span>}
                  </>
                ))}
              </div>
              <p className="text-body-lg mt-8 font-medium">{s.metric}</p>
            </div>
            <div className="space-y-4">
              {s.screenshots.map((src, i) => <img key={i} src={src} alt="" className="w-full border border-current/15" />)}
            </div>
          </div>
        </section>
      ))}
      <section className="px-6 md:px-10 lg:px-16 py-32 border-t border-current/15">
        <h2 className="text-display-md font-display mb-12">11개월 개발 타임라인</h2>
        <div className="space-y-4">
          {TIMELINE_2024_2025.map((t, i) => (
            <div key={i} className="grid grid-cols-12 gap-4 py-3 border-b border-current/10 text-body">
              <span className="col-span-2 text-meta opacity-60">{t.period}</span>
              <span className="col-span-3 font-medium">{t.phase}</span>
              <span className="col-span-7 opacity-80">{t.note}</span>
            </div>
          ))}
        </div>
      </section>
      <section className="px-6 md:px-10 lg:px-16 py-32 border-t border-current/15">
        <p className="text-meta opacity-60">SECONDARY</p>
        <h2 className="text-display-md font-display mt-4 mb-8">81 SYSTEM 인벤토리 (보조)</h2>
        <p className="text-meta opacity-60 mb-12">
          본인 작업 인벤토리. 메인 강조는 위 4 핵심. 클라이언트 작업·실험·진화 버전 포함.
        </p>
        <div className="grid md:grid-cols-2 gap-6">
          {INVENTORY_GROUPS.map(g => (
            <div key={g.name} className="border border-current/15 p-6">
              <p className="text-display-md font-display">{g.count}</p>
              <p className="text-body-lg font-medium mt-2">{g.name}</p>
              <p className="text-meta opacity-60 mt-2">SYSTEM {g.systemRange}</p>
            </div>
          ))}
        </div>
      </section>
    </>
  );
}
```

- [ ] **Step 2: 검증 + 커밋**

```bash
git add app/builder/scenarios
git commit -m "feat(scenarios): 4 핵심 풀 + 11개월 타임라인 + 81 인벤토리 보조 (분리 명시)"
```

---

## Phase 15 — `/builder/github` (옵션) + `/contact`

### Task 15.1: GitHub 84 카테고리

**Files:** Create `app/builder/github/page.tsx`

```tsx
import { GITHUB_CATEGORIES, GITHUB_PROFILE, GOLDEN_PRINCIPLES } from "@/lib/data/github";

export default function GitHub() {
  return (
    <>
      <section className="px-6 md:px-10 lg:px-16 pt-32 pb-16">
        <h1 className="text-display-lg font-display">GitHub 84 저장소</h1>
        <a href={GITHUB_PROFILE} target="_blank" rel="noreferrer" className="text-meta opacity-60 hover:opacity-100 mt-4 inline-block">
          {GITHUB_PROFILE} →
        </a>
      </section>
      <section className="px-6 md:px-10 lg:px-16 py-16 border-t border-current/15">
        <div className="grid md:grid-cols-2 gap-8">
          {GITHUB_CATEGORIES.map(g => (
            <div key={g.id} className="border border-current/15 p-8">
              <p className="text-display-md font-display">{g.count}</p>
              <p className="text-body-lg font-medium mt-2">{g.name}</p>
              <p className="text-meta opacity-60 mt-4">{g.examples.slice(0, 6).join(" · ")}</p>
            </div>
          ))}
        </div>
      </section>
      <section className="px-6 md:px-10 lg:px-16 py-32 border-t border-current/15">
        <h2 className="text-display-md font-display mb-12">12 Golden Principles</h2>
        <div className="grid md:grid-cols-3 gap-4 text-body">
          {GOLDEN_PRINCIPLES.map((p, i) => <div key={i} className="border-l border-current/30 pl-4 py-2"><span className="text-meta opacity-50 mr-3">{(i + 1).toString().padStart(2, "0")}</span>{p}</div>)}
        </div>
      </section>
    </>
  );
}
```

### Task 15.2: /contact

**Files:** Create `app/contact/page.tsx`

```tsx
import { Magnetic } from "@/components/motion/magnetic";
import { CONTACT_EMAIL, CTA_QUOTE } from "@/lib/data/home";

export default function Contact() {
  return (
    <section className="min-h-screen flex flex-col items-center justify-center px-6 py-32 text-center">
      <p className="text-meta mb-12 opacity-60">{CTA_QUOTE}</p>
      <Magnetic strength={0.5}>
        <a href={`mailto:${CONTACT_EMAIL}`} className="text-display-mega font-display hover:text-orange transition-colors break-all">
          {CONTACT_EMAIL}
        </a>
      </Magnetic>
      <div className="mt-24 text-meta opacity-60 space-y-3">
        <p><a href="mailto:junginsuai@gmail.com" className="hover:opacity-100">junginsuai@gmail.com (이직)</a></p>
        <p><a href="mailto:sbcyberpass@naver.com" className="hover:opacity-100">sbcyberpass@naver.com (블로그)</a></p>
        <p><a href="https://github.com/junginsu-make" target="_blank" rel="noreferrer" className="hover:opacity-100">github.com/junginsu-make</a></p>
        <p><a href="/resume.pdf" download className="hover:opacity-100">이력서 PDF 다운로드 →</a></p>
      </div>
    </section>
  );
}
```

- [ ] **Step 1: 두 페이지 생성 + 커밋**

```bash
git add app/builder/github app/contact
git commit -m "feat: /builder/github (옵션) + /contact"
```

---

## Phase 16 — 통합 검증

### Task 16.1: 다크/라이트 둘 다 풀 페이지 시각 검증 (수동)

- [ ] **Step 1: dev 서버 + 모든 페이지 라이트 모드 검증**

```bash
pnpm dev
```

브라우저에서 라이트 모드:
- `/`, `/about`, `/career`, `/marketing`, `/marketing/teaching`, `/marketing/content`, `/builder`, `/builder/[6 slug]`, `/builder/scenarios`, `/builder/github`, `/contact`
- 각 페이지: 폰트 가독성 / 대비 / 레이아웃 / 인터랙션 / 스크롤 / 모션 모두 작동

- [ ] **Step 2: 다크 모드 토글 후 모든 페이지 재검증**

다크 모드에서 동일 페이지 풀 검증.

- [ ] **Step 3: 발견된 이슈 메모 + 수정**

이슈 발견 시 fix → commit:

```bash
git commit -m "fix(theme): [이슈 설명]"
```

### Task 16.2: 모바일 시뮬레이션 (375px / 768px)

- [ ] **Step 1: dev tools mobile**

iPhone SE (375px) → iPad (768px) → 모든 페이지 검증.

- [ ] **Step 2: 발견 이슈 fix + 커밋**

### Task 16.3: Lighthouse 90+ (라이트·다크 각각)

- [ ] **Step 1: production 빌드**

```bash
pnpm build
pnpm start  # 또는 production 서버
```

- [ ] **Step 2: Lighthouse 검사**

dev tools → Lighthouse → 라이트 모드 검사 → 다크 모드 토글 후 재검사.

성능·접근성·SEO·베스트프랙티스 모두 90+ 확인.

- [ ] **Step 3: 부족 부분 fix**

성능 부족 시 이미지 최적화·LCP 개선·dynamic import.

### Task 16.4: 콘솔 에러 0 검증

모든 페이지 dev tools Console → 에러 0 확인.

### Task 16.5: production 빌드 + 최종 commit

```bash
pnpm build
# 빌드 성공 확인
git commit --allow-empty -m "chore: v3 production build success + manual verification complete"
git tag v3.0.0
```

---

## Self-Review

> 이 plan을 fresh eyes로 다시 점검합니다.

### Spec coverage

| Spec 섹션 | Plan 매칭 |
|---|---|
| §0 절대 원칙 (5 룰 + 후속 5 + 5 자문) | Phase 16 검증 단계에서 매 페이지 5 자문 통과 확인 |
| §1 v2 실패 원인 | Phase 3 모션 primitives + Task 0.4 디자인 토큰으로 구조적 해결 |
| §2 확정 결정 16 항목 | Phase 0~3 (foundation) + Phase 4 theme + Phase 13 트렌디 디테일 |
| §3 정정된 정량 지표 | Phase 1 데이터 (84/4/6/17+ 등 모두 데이터 객체에 박제 + 테스트로 무결성) |
| §4 IA 16 라우트 | Phase 6~15 모든 라우트 커버 (홈·about·career·marketing·teaching·content·builder·6 slug·scenarios·github·contact) |
| §5 디자인 토큰 (다크+라이트) | Phase 0.4 globals.css에 풀 정의 |
| §6 모션 14 패턴 | Phase 3 primitives (Lenis·ScrollTrigger·Magnetic·Counter·KineticText·ColorSweep·PinSection) |
| §7 반전 인터랙션 4 모드 | Theme provider + globals.css view-transition + mix-blend-difference |
| §8.1~8.11 페이지 명세 | Phase 6~15 페이지별 일대일 매칭 |
| §9 시각화 | Phase 9 Recharts (KpiCharts) + Phase 14 81 인벤토리 (Visx 트리맵 — 옵션 보강) |
| §10 기술 스택 | Phase 0 deps 설치에 모두 포함 |
| §11 URL 캡처 | Phase 2.3 capture-urls.ts |
| §12 자료 매핑 | Phase 1 데이터 import + Phase 2.1 copy-assets.sh |
| §13 검증 KPI | Phase 16 통합 검증 |
| §14 범위 외 | 강의 mp4 / 교육자료 / TMON ROAS / NGO 마음하나 모두 언급 X (정확) |

### Placeholder 스캔

- "TBD", "TODO", "later" 검색: 없음
- "Add appropriate error handling": 없음 (구체적 try/catch 또는 ok flag 사용)
- "Similar to Task N": 없음 (각 task 풀 코드)

### Type 일관성

- `SaaSDetail` 타입 lib/data/saas.ts에 정의 → builder 컴포넌트에서 import 일관
- `Company`, `ImpactStream` 타입 lib/data/career.ts → /career 컴포넌트 일관
- `useTheme()` lib/theme.tsx → ThemeToggle 일관

### 미보강 (구현 단계 결정)

- `/playground` (옵션) — Phase 미포함 (시간 여유 시 추가)
- 81 SYSTEM Visx 트리맵 디테일 — Phase 14에 placeholder만, 데이터로 대체. 시간 여유 시 보강
- GA4 분석 부분 별도 스크린샷 — 사용자 확인 후 보강 (spec §14 미정 항목)
- GitHub 신규 3 저장소 — 빌드 시 `gh repo list` 직접 호출로 보강 (Phase 1.7에서 placeholder)
- 콘텐츠 url 캡처 우선순위 — Phase 2.3 capture-urls.ts에서 결정

이 plan은 spec을 풀 커버하며, 미정 항목은 명시적으로 분리.

---

## Plan complete

**파일**: `/home/a20616050/projects/A/junginsu-portfolio-v3/docs/superpowers/plans/2026-05-04-portfolio-v3.md`

**전체 phase**: 16 (Phase 0~16)
**전체 task**: 약 60+
**예상 소요**: Asset 캡처 60~90분 + 빌드 5~7일 (본격 작업)

**다음 단계 — 실행 방식 선택**:

**1. Subagent-Driven (recommended)** — fresh subagent per task + 두 단계 리뷰 (각 task 완료 시 review subagent dispatch)

**2. Inline Execution** — 이 세션에서 task 일괄 실행 + 체크포인트 리뷰

어느 방식?
